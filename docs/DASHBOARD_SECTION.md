# Раздел Dashboard (/dashboard)

## Общее описание

Dashboard — главная страница приложения после авторизации. Это центр управления повседневным самочувствием, где пользователь может:
- Просматривать активности на сегодня
- Записывать показатели трекеров (настроение, эмоции, стресс, энергия)
- Видеть статистику выполнения активностей по типам воздействия
- Получать персональные рекомендации

---

## Структура файлов

```
src/
├── pages/
│   └── Dashboard.tsx                    # Главная страница
├── components/dashboard/
│   ├── DashboardHeader.tsx              # Заголовок с приветствием
│   ├── QuickTrackerCard.tsx             # Карточка трекеров
│   ├── TodayActivitiesCard.tsx          # Активности на сегодня
│   ├── ActivityImpactCards.tsx          # Диаграммы по типам воздействия
│   ├── InsightsPreview.tsx              # Превью рекомендаций
│   └── trackers/
│       ├── MoodSlider.tsx               # Слайдер настроения
│       ├── EmotionsSelector.tsx         # Выбор эмоций
│       ├── StressSlider.tsx             # Слайдер стресса
│       ├── AnxietySlider.tsx            # Слайдер тревожности
│       ├── EnergySlider.tsx             # Слайдер энергии
│       └── SatisfactionSliders.tsx      # Слайдеры удовлетворённости
```

---

## Архитектура компонентов

```
Dashboard.tsx
├── AppLayout
│   ├── DashboardHeader
│   ├── Grid Layout
│   │   ├── Left Column (lg:col-span-2)
│   │   │   ├── TodayActivitiesCard
│   │   │   ├── QuickTrackerCard
│   │   │   ├── Navigation Buttons
│   │   │   └── InsightsPreview (mobile)
│   │   └── Right Column (desktop only)
│   │       ├── ActivityImpactCards
│   │       └── InsightsPreview
│   └── ActivityImpactCards (mobile)
```

---

## Модель данных

### TrackerData (интерфейс состояния трекеров)

```typescript
interface TrackerData {
  moodScore: number;                    // Оценка настроения (-5 до +5)
  selectedEmotions: Array<{
    label: string;                      // Название эмоции
    intensity: number;                  // Интенсивность (1-3)
    category: string;                   // Категория (positive/negative)
  }>;
  stressLevel: number;                  // Уровень стресса (0-10)
  anxietyLevel: number;                 // Уровень тревожности (0-10)
  energyLevel: number;                  // Уровень энергии (-5 до +5)
  processSatisfaction: number;          // Удовлетворённость процессом (0-10)
  resultSatisfaction: number;           // Удовлетворённость результатом (0-10)
}
```

### Таблица tracker_entries

```sql
CREATE TABLE tracker_entries (
  id UUID PRIMARY KEY,
  user_id UUID REFERENCES auth.users,
  entry_date DATE,                      -- Дата записи
  entry_time TIME,                      -- Время записи
  mood_score INTEGER,                   -- Настроение
  stress_level INTEGER,                 -- Стресс
  anxiety_level INTEGER,                -- Тревожность
  energy_level INTEGER,                 -- Энергия
  process_satisfaction INTEGER,         -- Удовлетворённость процессом
  result_satisfaction INTEGER,          -- Удовлетворённость результатом
  created_at TIMESTAMPTZ
);
```

### Таблица tracker_emotions

```sql
CREATE TABLE tracker_emotions (
  id UUID PRIMARY KEY,
  tracker_entry_id UUID REFERENCES tracker_entries,
  emotion_label TEXT,                   -- Название эмоции
  intensity INTEGER,                    -- Интенсивность (1-3)
  category TEXT                         -- positive/negative
);
```

---

## Описание компонентов

### 1. DashboardHeader

**Назначение:** Приветственный заголовок с именем пользователя и текущей датой.

**Функционал:**
- Отображает "Доброе утро/день/вечер, [Имя]"
- Показывает текущую дату

---

### 2. TodayActivitiesCard

**Назначение:** Карточка с активностями на текущий день.

**Визуальный вид:**
```
┌─────────────────────────────────────────────────┐
│ 📅 Сегодняшние активности                       │
│ Понедельник, 4 января             [+ Добавить] │
├─────────────────────────────────────────────────┤
│ 🌅 Раннее утро (5:00-9:00)                      │
│   ├── ☑️ 🧘 Медитация                07:00      │
│   └── ☐  🏃 Утренняя пробежка        08:00      │
│                                                  │
│ ☕ Позднее утро (9:00-12:00)                    │
│   └── ☐  💼 Рабочие задачи          10:00      │
│                                                  │
│ ☀️ Полдень (12:00-14:00)                        │
│   └── ☐  🍽️ Обед                    13:00      │
├─────────────────────────────────────────────────┤
│ 2/8 выполнено                                   │
└─────────────────────────────────────────────────┘
```

**Группировка по временным слотам:**
- 🌅 Раннее утро (5:00-9:00)
- ☕ Позднее утро (9:00-12:00)
- ☀️ Полдень (12:00-14:00)
- 🌤️ Дневное время (14:00-17:00)
- 🌆 Вечер (17:00-21:00)
- 🌙 Ночь (21:00-5:00)
- 📌 В любое время

**Логика:**
1. Загружает активности на сегодня из `activities`
2. Группирует по `TIME_SLOTS` через `filterActivitiesBySlot()`
3. Подписывается на Realtime обновления
4. Позволяет отмечать выполнение через checkbox

---

### 3. QuickTrackerCard

**Назначение:** Основной инструмент для записи самочувствия.

**Визуальный вид:**
```
┌─────────────────────────────────────────────────┐
│ 📊 Трекеры                                      │
├─────────────────────────────────────────────────┤
│ ▼ Настроение и эмоции                           │
│   ┌─────────────────────────────────────────┐   │
│   │ Как ваше настроение?                    │   │
│   │ 😢 ─────●───────────── 😊               │   │
│   │           Хорошо (+2)                   │   │
│   ├─────────────────────────────────────────┤   │
│   │ Какие эмоции вы испытываете?            │   │
│   │ [😊 Радость] [😌 Спокойствие] [+3 ещё]  │   │
│   └─────────────────────────────────────────┘   │
│   [         Сохранить запись         ]          │
│                                                  │
│ ▶ Состояние                                     │
│ ▶ Удовлетворённость                             │
└─────────────────────────────────────────────────┘
```

**Структура (Accordion):**

#### Секция 1: Настроение и эмоции
- **MoodSlider**: Шкала от -5 до +5
  - -5: 😢 Очень плохо
  - 0: 😐 Нейтрально
  - +5: 😊 Отлично
- **EmotionsSelector**: Выбор эмоций с интенсивностью

#### Секция 2: Состояние
- **StressSlider**: Уровень стресса (0-10)
- **AnxietySlider**: Уровень тревожности (0-10)
- **EnergySlider**: Уровень энергии (-5 до +5)

#### Секция 3: Удовлетворённость
- **SatisfactionSliders**:
  - Удовлетворённость процессом (0-10)
  - Удовлетворённость результатом (0-10)

**Алгоритм сохранения:**

```typescript
const handleSaveMoodAndEmotions = async () => {
  // 1. Создать запись в tracker_entries
  const { data: entry } = await supabase
    .from('tracker_entries')
    .insert({
      user_id: user.id,
      entry_date: today,
      entry_time: now,
      mood_score: trackerData.moodScore,
    })
    .select()
    .single();

  // 2. Сохранить эмоции в tracker_emotions
  if (trackerData.selectedEmotions.length > 0) {
    const emotions = trackerData.selectedEmotions.map(e => ({
      tracker_entry_id: entry.id,
      emotion_label: e.label,
      intensity: e.intensity,
      category: e.category,
    }));
    
    await supabase.from('tracker_emotions').insert(emotions);
  }

  // 3. Сбросить состояние формы
  // 4. Вызвать onEntrySaved() для обновления Dashboard
};
```

---

### 4. ActivityImpactCards

**Назначение:** Визуализация статистики активностей по типам воздействия.

**Визуальный вид:**
```
┌─────────────┐ ┌─────────────┐
│     ◐       │ │     ◑       │
│   75%       │ │   50%       │
│ Восст-щие   │ │ Истощающие  │
└─────────────┘ └─────────────┘
┌─────────────┐ ┌─────────────┐
│     ○       │ │     ◔       │
│    —        │ │   25%       │
│ Нейтральные │ │ Смешанные   │
└─────────────┘ └─────────────┘
```

**Типы воздействия и цвета:**

| Тип | HSL цвет | Описание |
|-----|----------|----------|
| `restoring` | `hsl(127 40% 62%)` | Восстанавливающие |
| `depleting` | `hsl(0 72% 60%)` | Истощающие |
| `neutral` | `hsl(210 40% 60%)` | Нейтральные |
| `mixed` | `hsl(31 85% 62%)` | Смешанные |

**Логика расчёта:**
```typescript
const calculateStats = (impactType) => {
  const typeActivities = activities.filter(a => a.impact_type === impactType);
  const total = typeActivities.length;
  const completed = typeActivities.filter(a => a.status === 'completed').length;
  const percentage = total > 0 ? Math.round((completed / total) * 100) : 0;
  
  return { total, completed, percentage };
};
```

**Логика отображения диаграммы:**
1. Нет активностей → серый круг
2. Есть, но 0 выполнено → светлый цвет типа
3. Есть выполненные → часть круга насыщенная

---

### 5. InsightsPreview

**Назначение:** Превью персональных рекомендаций.

**Визуальный вид:**
```
┌─────────────────────────────────────────────────┐
│ ⚠️  2 рекомендации для вас                      │
│                                                  │
│ 🧘 High stress: Попробуйте медитацию            │
│                                                  │
│ [    Посмотреть рекомендации    ]               │
└─────────────────────────────────────────────────┘
```

**Логика:**
1. Загружает рекомендации из `user_recommendations`
2. Фильтрует только активные (не принятые, не отклонённые)
3. Показывает топ-1 по приоритету
4. Высокий приоритет → красная тема карточки
5. Обычный приоритет → фиолетовая тема

---

## Потоки данных

### Pull-to-Refresh

Dashboard поддерживает жест "потянуть для обновления":

```typescript
const { containerRef, pullDistance, isRefreshing, shouldShowIndicator } = usePullToRefresh({
  onRefresh: handleRefresh,
});
```

### Realtime подписка

```typescript
const setupRealtimeSubscription = () => {
  const channel = supabase
    .channel('activities-realtime-dashboard')
    .on(
      'postgres_changes',
      {
        event: '*',
        schema: 'public',
        table: 'activities',
        filter: `user_id=eq.${user?.id}`
      },
      (payload) => {
        fetchTodayActivities();
      }
    )
    .subscribe();

  return () => supabase.removeChannel(channel);
};
```

### Пользовательское событие обновления

```typescript
// Слушаем событие для принудительного обновления
window.addEventListener('activity-updated', handleActivityUpdate);

// Триггер события из других компонентов
window.dispatchEvent(new CustomEvent('activity-updated'));
```

---

## Каталог эмоций

### Позитивные эмоции

| Эмоция | Emoji | Ключ локализации |
|--------|-------|------------------|
| Радость | 😊 | `emotions.joy` |
| Спокойствие | 😌 | `emotions.calm` |
| Благодарность | 🙏 | `emotions.gratitude` |
| Энтузиазм | 🤩 | `emotions.enthusiasm` |
| Надежда | 🌈 | `emotions.hope` |
| Любовь | ❤️ | `emotions.love` |
| Гордость | 😎 | `emotions.pride` |
| Удовлетворение | 😊 | `emotions.satisfaction` |

### Негативные эмоции

| Эмоция | Emoji | Ключ локализации |
|--------|-------|------------------|
| Грусть | 😢 | `emotions.sadness` |
| Тревога | 😰 | `emotions.anxiety` |
| Злость | 😠 | `emotions.anger` |
| Страх | 😨 | `emotions.fear` |
| Разочарование | 😞 | `emotions.disappointment` |
| Одиночество | 🥺 | `emotions.loneliness` |
| Стыд | 😳 | `emotions.shame` |
| Вина | 😔 | `emotions.guilt` |

---

## Интеграция с другими разделами

| Кнопка | Навигация |
|--------|-----------|
| "+ Добавить" | `/calendar` |
| "История трекеров" | `/tracker-history` |
| "Шаблоны активностей" | `/activity-templates` |
| "Посмотреть рекомендации" | `/recommendations` |

---

## Адаптивный дизайн

### Desktop (lg+)
- 3-колоночная сетка
- Левая колонка (2/3): активности + трекеры
- Правая колонка (1/3): статистика + рекомендации

### Mobile/Tablet
- Однаколоночный layout
- InsightsPreview под трекерами
- ActivityImpactCards в самом низу
