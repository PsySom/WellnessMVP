# Раздел Insights (/insights)

## Общее описание

Insights — аналитический дашборд с визуализацией данных о самочувствии. Страница объединяет:
- Обзорные метрики (карточки KPI)
- Графики трендов настроения
- Анализ эмоций
- Графики стресса и тревожности
- Паттерны энергии
- Статистика активностей
- Персональные рекомендации

---

## Структура файлов

```
src/
├── pages/
│   └── Insights.tsx                     # Главная страница
├── components/insights/
│   ├── OverviewCards.tsx                # Обзорные карточки KPI
│   ├── MoodTrendsChart.tsx              # График трендов настроения
│   ├── MoodDistribution.tsx             # Распределение настроения (pie)
│   ├── TopEmotions.tsx                  # Топ эмоций
│   ├── EmotionBalance.tsx               # Баланс позитивных/негативных
│   ├── StressAnxietyChart.tsx           # График стресса и тревожности
│   ├── EnergyPatterns.tsx               # Паттерны энергии
│   ├── SatisfactionCharts.tsx           # Графики удовлетворённости
│   ├── ActivityCompletion.tsx           # Выполнение активностей
│   ├── ActivityBreakdown.tsx            # Разбивка активностей по категориям
│   ├── PatternCards.tsx                 # Карточки паттернов
│   └── RecommendationsCard.tsx          # Карточка рекомендаций
```

---

## Архитектура страницы

```
┌─────────────────────────────────────────────────────────────────┐
│  📊 Аналитика                                      [📥 Экспорт] │
│  1 янв - 4 янв 2026                                             │
├─────────────────────────────────────────────────────────────────┤
│  [День] [Неделя] [Месяц] [3 месяца] [Год]                       │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐                │
│  │  +2.3   │ │   42    │ │   28    │ │   85%   │                │
│  │ Среднее │ │ Записей │ │Активн-й │ │Выполнено│                │
│  │настрое- │ │ трекера │ │         │ │         │                │
│  │  ние    │ │         │ │         │ │         │                │
│  └─────────┘ └─────────┘ └─────────┘ └─────────┘                │
│                                                                  │
│  📈 НАСТРОЕНИЕ                                                   │
│  ┌─────────────────────────────────┐ ┌─────────────────────────┐ │
│  │  Тренды настроения              │ │  Распределение          │ │
│  │  ▁▂▃▅▆▇█▇▆▅▄▃▂▃▄▅              │ │      ◐                  │ │
│  │  Пн Вт Ср Чт Пт Сб Вс          │ │  Хорошо   Норма  Плохо  │ │
│  └─────────────────────────────────┘ └─────────────────────────┘ │
│                                                                  │
│  🎭 ЭМОЦИОНАЛЬНЫЙ ЛАНДШАФТ                                       │
│  ┌─────────────────────────────────┐ ┌─────────────────────────┐ │
│  │  Топ эмоций                     │ │  Баланс эмоций          │ │
│  │  1. 😊 Радость (24)             │ │  Позитивные ████████ 65%│ │
│  │  2. 😌 Спокойствие (18)         │ │  Негативные ████ 35%    │ │
│  │  3. 😰 Тревога (12)             │ │                         │ │
│  └─────────────────────────────────┘ └─────────────────────────┘ │
│                                                                  │
│  😰 СТРЕСС И ТРЕВОЖНОСТЬ                                         │
│  ┌─────────────────────────────────────────────────────────────┐ │
│  │  ▁▂▃▅▆▅▄▃▂▁▂▃▄▅▆▅▄▃            Стресс ────                 │ │
│  │  ▂▃▄▅▄▃▂▁▂▃▄▃▂▁▂▃              Тревожность ----            │ │
│  └─────────────────────────────────────────────────────────────┘ │
│                                                                  │
│  ⚡ ЭНЕРГИЯ И УДОВЛЕТВОРЁННОСТЬ                                  │
│  ┌─────────────────────────────────┐ ┌─────────────────────────┐ │
│  │  Паттерны энергии               │ │  Удовлетворённость      │ │
│  │  Утро: ████████ +3              │ │  Процесс: ▰▰▰▰▰▰▰▱ 7.2  │ │
│  │  День:  ██████ +2               │ │  Результ: ▰▰▰▰▰▰▱▱ 6.8  │ │
│  │  Вечер: ████ +1                 │ │                         │ │
│  └─────────────────────────────────┘ └─────────────────────────┘ │
│                                                                  │
│  📅 АКТИВНОСТИ                                                   │
│  ┌─────────────────────────────────┐ ┌─────────────────────────┐ │
│  │  Выполнение                     │ │  По категориям          │ │
│  │  ████████████████████ 85%       │ │  🧘 Медитация    28%    │ │
│  │  Выполнено: 28 / 33             │ │  🏃 Спорт        24%    │ │
│  │                                 │ │  📚 Обучение    18%    │ │
│  └─────────────────────────────────┘ └─────────────────────────┘ │
│                                                                  │
│  ┌─────────────────────────────────┐ ┌─────────────────────────┐ │
│  │  🔍 Паттерны                    │ │  💡 Рекомендации        │ │
│  │  • Лучшее настроение: утро      │ │  • Добавьте больше      │ │
│  │  • Пик стресса: среда           │ │    восстанавливающих    │ │
│  │  • Энергия падает к вечеру      │ │    активностей          │ │
│  └─────────────────────────────────┘ └─────────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
```

---

## Периоды анализа

```typescript
type Period = 'day' | 'week' | 'month' | '3months' | 'year';

const getPeriodStartDate = (period: Period): Date => {
  const now = new Date();
  const startDate = new Date();
  
  switch (period) {
    case 'day':
      startDate.setDate(now.getDate() - 1);
      break;
    case 'week':
      startDate.setDate(now.getDate() - 7);
      break;
    case 'month':
      startDate.setMonth(now.getMonth() - 1);
      break;
    case '3months':
      startDate.setMonth(now.getMonth() - 3);
      break;
    case 'year':
      startDate.setFullYear(now.getFullYear() - 1);
      break;
  }
  
  return startDate;
};
```

---

## Загрузка данных

```typescript
const fetchData = async () => {
  const startDate = getPeriodStartDate(period);

  // 1. Записи трекера с эмоциями
  const { data: trackerEntries } = await supabase
    .from('tracker_entries')
    .select(`*, tracker_emotions (*)`)
    .eq('user_id', user?.id)
    .gte('entry_date', startDate.toISOString().split('T')[0])
    .order('entry_date', { ascending: true });

  // 2. Активности
  const { data: activities } = await supabase
    .from('activities')
    .select('*')
    .eq('user_id', user?.id)
    .gte('date', startDate.toISOString().split('T')[0])
    .order('date', { ascending: true });

  // 3. Сессии журнала
  const { data: journalSessions } = await supabase
    .from('journal_sessions')
    .select('*')
    .eq('user_id', user?.id)
    .gte('started_at', startDate.toISOString())
    .order('started_at', { ascending: true });

  setData({
    trackerEntries: trackerEntries || [],
    activities: activities || [],
    journalSessions: journalSessions || [],
  });
};
```

---

## Компоненты аналитики

### 1. OverviewCards

Обзорные карточки с ключевыми метриками:

| Метрика | Описание | Расчёт |
|---------|----------|--------|
| Среднее настроение | Средний mood_score | `sum(mood_score) / count` |
| Записей трекера | Количество записей | `trackerEntries.length` |
| Активностей | Всего активностей | `activities.length` |
| % выполнено | Процент выполненных | `completed / total * 100` |

---

### 2. MoodTrendsChart

График трендов настроения (Line Chart):

```typescript
// Группировка по дням
const groupByDay = (entries: TrackerEntry[]) => {
  return entries.reduce((acc, entry) => {
    const date = entry.entry_date;
    if (!acc[date]) acc[date] = [];
    acc[date].push(entry.mood_score);
    return acc;
  }, {});
};

// Расчёт среднего за день
const dailyAverages = Object.entries(groupByDay(entries)).map(([date, scores]) => ({
  date,
  avgMood: scores.reduce((a, b) => a + b, 0) / scores.length
}));
```

---

### 3. MoodDistribution

Круговая диаграмма распределения настроения:

| Категория | Диапазон | Цвет |
|-----------|----------|------|
| Отлично | +3 до +5 | Зелёный |
| Хорошо | +1 до +2 | Светло-зелёный |
| Нейтрально | -1 до +1 | Жёлтый |
| Плохо | -3 до -1 | Оранжевый |
| Очень плохо | -5 до -3 | Красный |

---

### 4. TopEmotions

Список наиболее частых эмоций:

```typescript
const getTopEmotions = (entries: TrackerEntry[]) => {
  const emotionCounts: Record<string, number> = {};
  
  entries.forEach(entry => {
    entry.tracker_emotions?.forEach(emotion => {
      emotionCounts[emotion.emotion_label] = 
        (emotionCounts[emotion.emotion_label] || 0) + 1;
    });
  });
  
  return Object.entries(emotionCounts)
    .sort((a, b) => b[1] - a[1])
    .slice(0, 5)
    .map(([label, count]) => ({ label, count }));
};
```

---

### 5. EmotionBalance

Баланс позитивных и негативных эмоций:

```typescript
const calculateBalance = (entries: TrackerEntry[]) => {
  let positive = 0;
  let negative = 0;
  
  entries.forEach(entry => {
    entry.tracker_emotions?.forEach(emotion => {
      if (emotion.category === 'positive') positive++;
      else if (emotion.category === 'negative') negative++;
    });
  });
  
  const total = positive + negative;
  return {
    positivePercent: total > 0 ? (positive / total) * 100 : 50,
    negativePercent: total > 0 ? (negative / total) * 100 : 50,
  };
};
```

---

### 6. StressAnxietyChart

Двойной линейный график стресса и тревожности:

- **Стресс** — сплошная линия
- **Тревожность** — пунктирная линия

Шкала: 0-10

---

### 7. EnergyPatterns

Паттерны энергии по времени суток:

```typescript
const getEnergyByTimeOfDay = (entries: TrackerEntry[]) => {
  const groups = {
    morning: [],   // 5:00-12:00
    afternoon: [], // 12:00-17:00
    evening: [],   // 17:00-21:00
    night: [],     // 21:00-5:00
  };
  
  entries.forEach(entry => {
    const hour = parseInt(entry.entry_time.split(':')[0]);
    const timeOfDay = getTimeOfDay(hour);
    groups[timeOfDay].push(entry.energy_level);
  });
  
  return Object.entries(groups).map(([time, levels]) => ({
    timeOfDay: time,
    avgEnergy: levels.length > 0 
      ? levels.reduce((a, b) => a + b, 0) / levels.length 
      : 0
  }));
};
```

---

### 8. SatisfactionCharts

Графики удовлетворённости:

- **Удовлетворённость процессом** (process_satisfaction)
- **Удовлетворённость результатом** (result_satisfaction)

Визуализация: горизонтальные progress bars или gauge charts.

---

### 9. ActivityCompletion

Статистика выполнения активностей:

```typescript
const getCompletionStats = (activities: Activity[]) => {
  const total = activities.length;
  const completed = activities.filter(a => a.status === 'completed').length;
  const cancelled = activities.filter(a => a.status === 'cancelled').length;
  const planned = activities.filter(a => a.status === 'planned').length;
  
  return {
    total,
    completed,
    cancelled,
    planned,
    completionRate: total > 0 ? (completed / total) * 100 : 0
  };
};
```

---

### 10. ActivityBreakdown

Разбивка активностей по категориям (Pie/Bar Chart):

```typescript
const getBreakdownByCategory = (activities: Activity[]) => {
  const categories: Record<string, number> = {};
  
  activities.forEach(activity => {
    categories[activity.category] = (categories[activity.category] || 0) + 1;
  });
  
  return Object.entries(categories)
    .map(([category, count]) => ({ category, count }))
    .sort((a, b) => b.count - a.count);
};
```

---

### 11. PatternCards

Автоматически обнаруженные паттерны:

- "Лучшее настроение по утрам"
- "Пик стресса в середине недели"
- "Энергия падает после 18:00"
- "Больше позитивных эмоций по выходным"

---

### 12. RecommendationsCard

Персональные рекомендации на основе данных:

- "Добавьте больше восстанавливающих активностей"
- "Попробуйте медитацию перед сном"
- "Рассмотрите упражнения для снижения стресса"

---

## Экспорт данных

```typescript
const handleExport = () => {
  const exportData = {
    period,
    exported_at: new Date().toISOString(),
    trackerEntries: data.trackerEntries,
    activities: data.activities,
    journalSessions: data.journalSessions,
  };

  const blob = new Blob(
    [JSON.stringify(exportData, null, 2)], 
    { type: 'application/json' }
  );
  
  downloadFile(blob, `insights-${period}-${date}.json`);
};
```

---

## Условный рендеринг секций

```typescript
// Эмоции показываются только если есть данные
{data.trackerEntries.some(e => e.tracker_emotions?.length > 0) && (
  <div>
    <h2>Эмоциональный ландшафт</h2>
    <TopEmotions entries={data.trackerEntries} />
    <EmotionBalance entries={data.trackerEntries} />
  </div>
)}

// Стресс/тревожность показываются только если есть данные
{data.trackerEntries.some(e => e.stress_level !== null) && (
  <StressAnxietyChart entries={data.trackerEntries} />
)}

// Активности показываются только если есть данные
{data.activities.length > 0 && (
  <div>
    <ActivityCompletion activities={data.activities} />
    <ActivityBreakdown activities={data.activities} />
  </div>
)}
```

---

## Адаптивный дизайн

| Viewport | Layout |
|----------|--------|
| Desktop (lg+) | 2-3 колонки, полноразмерные графики |
| Tablet (md) | 2 колонки, компактные графики |
| Mobile | 1 колонка, scrollable cards |
