# Раздел Onboarding (/onboarding)

## Общее описание

Onboarding — процесс первоначальной настройки приложения для новых пользователей. Многошаговый wizard собирает базовую информацию о пользователе и его предпочтениях.

---

## Структура файлов

```
src/
├── pages/
│   └── Onboarding.tsx                   # Главная страница wizard
├── components/onboarding/
│   ├── WelcomeScreen.tsx                # Экран приветствия
│   ├── BasicInfoScreen.tsx              # Базовая информация
│   ├── GoalsScreen.tsx                  # Выбор целей
│   ├── RemindersScreen.tsx              # Настройка напоминаний
│   ├── ReadyScreen.tsx                  # Финальный экран
│   └── ProgressIndicator.tsx            # Индикатор прогресса
```

---

## Архитектура Wizard

```
┌─────────────────────────────────────────────────────────────────┐
│                                                                  │
│                     ●───○───○───○───○                           │
│                     1   2   3   4   5                           │
│                                                                  │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│                        WELCOME                                   │
│                                                                  │
│                          🌟                                      │
│                                                                  │
│                 Добро пожаловать в                              │
│                    MindBalance                                   │
│                                                                  │
│        Ваш персональный помощник для                            │
│        отслеживания самочувствия и                              │
│        развития здоровых привычек                               │
│                                                                  │
│               [    Начать    ]                                  │
│                                                                  │
│              [Пропустить настройку]                             │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

---

## Модель данных

```typescript
interface OnboardingData {
  fullName: string;                      // Имя пользователя
  age: number | null;                    // Возраст
  gender: string;                        // Пол
  goals: string[];                       // Выбранные цели
  trackerFrequency: number;              // Частота трекера (раз в день)
  trackerTimes: string[];                // Время трекера ['09:00', '21:00']
  morningReflectionEnabled: boolean;     // Утренняя рефлексия
  morningReflectionTime: string;         // Время утренней рефлексии
  eveningReflectionEnabled: boolean;     // Вечерняя рефлексия
  eveningReflectionTime: string;         // Время вечерней рефлексии
}

// Начальное состояние
const initialData: OnboardingData = {
  fullName: user?.user_metadata?.full_name || '',
  age: null,
  gender: '',
  goals: [],
  trackerFrequency: 2,
  trackerTimes: ['09:00', '21:00'],
  morningReflectionEnabled: true,
  morningReflectionTime: '08:00',
  eveningReflectionEnabled: true,
  eveningReflectionTime: '22:00',
};
```

---

## Экраны онбординга

### Screen 1: Welcome

```
┌─────────────────────────────────────────┐
│              ●───○───○───○───○          │
│                                         │
│                   🌟                    │
│                                         │
│          Добро пожаловать в             │
│             MindBalance                 │
│                                         │
│   Ваш персональный помощник для         │
│   отслеживания самочувствия             │
│                                         │
│          [    Начать    ]               │
│                                         │
│        [Пропустить настройку]           │
└─────────────────────────────────────────┘
```

**Компонент:** `WelcomeScreen`

```typescript
interface WelcomeScreenProps {
  onNext: () => void;
  onSkip: () => void;
}
```

---

### Screen 2: Basic Info

```
┌─────────────────────────────────────────┐
│              ○───●───○───○───○          │
│                                         │
│          Расскажите о себе              │
│                                         │
│   Имя                                   │
│   ┌─────────────────────────────────┐   │
│   │ John Doe                        │   │
│   └─────────────────────────────────┘   │
│                                         │
│   Возраст                               │
│   ┌─────────────────────────────────┐   │
│   │ 28                              │   │
│   └─────────────────────────────────┘   │
│                                         │
│   Пол                                   │
│   ○ Мужской  ○ Женский  ○ Другой       │
│   ○ Предпочитаю не указывать           │
│                                         │
│   [← Назад]              [Далее →]      │
└─────────────────────────────────────────┘
```

**Компонент:** `BasicInfoScreen`

```typescript
interface BasicInfoScreenProps {
  data: OnboardingData;
  onUpdate: (updates: Partial<OnboardingData>) => void;
  onNext: () => void;
  onBack: () => void;
  step: number;
}
```

---

### Screen 3: Goals

```
┌─────────────────────────────────────────┐
│              ○───○───●───○───○          │
│                                         │
│           Ваши цели                     │
│                                         │
│   Выберите, что важно для вас:          │
│                                         │
│   ☑️ Снизить стресс                     │
│   ☑️ Улучшить сон                       │
│   ☐ Развить привычки                    │
│   ☐ Повысить энергию                    │
│   ☑️ Управлять тревожностью             │
│   ☐ Улучшить настроение                 │
│   ☐ Повысить продуктивность             │
│   ☐ Самопознание                        │
│                                         │
│   [← Назад]              [Далее →]      │
└─────────────────────────────────────────┘
```

**Компонент:** `GoalsScreen`

**Доступные цели:**
```typescript
const goals = [
  { id: 'reduce_stress', label: t('goals.reduceStress'), emoji: '😌' },
  { id: 'improve_sleep', label: t('goals.improveSleep'), emoji: '😴' },
  { id: 'build_habits', label: t('goals.buildHabits'), emoji: '✅' },
  { id: 'increase_energy', label: t('goals.increaseEnergy'), emoji: '⚡' },
  { id: 'manage_anxiety', label: t('goals.manageAnxiety'), emoji: '🧘' },
  { id: 'improve_mood', label: t('goals.improveMood'), emoji: '😊' },
  { id: 'boost_productivity', label: t('goals.boostProductivity'), emoji: '🚀' },
  { id: 'self_awareness', label: t('goals.selfAwareness'), emoji: '🪞' },
];
```

---

### Screen 4: Reminders

```
┌─────────────────────────────────────────┐
│              ○───○───○───●───○          │
│                                         │
│        Настройка напоминаний            │
│                                         │
│   Частота трекера (раз в день)          │
│   ┌──────────────────────┐              │
│   │ 2                  ▼ │              │
│   └──────────────────────┘              │
│                                         │
│   Время напоминаний                     │
│   [09:00] [21:00]                       │
│                                         │
│   ─────────────────────────────         │
│                                         │
│   🌅 Утренняя рефлексия                 │
│   [✓] Включено    Время: [08:00]        │
│                                         │
│   🌙 Вечерняя рефлексия                 │
│   [✓] Включено    Время: [22:00]        │
│                                         │
│   [← Назад]              [Далее →]      │
└─────────────────────────────────────────┘
```

**Компонент:** `RemindersScreen`

---

### Screen 5: Ready

```
┌─────────────────────────────────────────┐
│              ○───○───○───○───●          │
│                                         │
│                  🎉                     │
│                                         │
│            Всё готово!                  │
│                                         │
│      Вы готовы начать свой путь         │
│      к лучшему самочувствию             │
│                                         │
│         [    Начать    ]                │
│                                         │
└─────────────────────────────────────────┘
```

**Компонент:** `ReadyScreen`

```typescript
interface ReadyScreenProps {
  onComplete: () => void;
}
```

---

## Логика навигации

```typescript
const [step, setStep] = useState(0);
const [isAnimating, setIsAnimating] = useState(false);
const [direction, setDirection] = useState<'forward' | 'backward'>('forward');

const goToNext = () => {
  setDirection('forward');
  setIsAnimating(true);
  setTimeout(() => {
    setStep(prev => prev + 1);
    setIsAnimating(false);
  }, 300);
};

const goToPrevious = () => {
  setDirection('backward');
  setIsAnimating(true);
  setTimeout(() => {
    setStep(prev => prev - 1);
    setIsAnimating(false);
  }, 300);
};
```

---

## Анимации переходов

```tsx
<div
  className={`transition-all duration-300 ease-in-out ${
    isAnimating
      ? direction === 'forward'
        ? 'opacity-0 -translate-x-8'
        : 'opacity-0 translate-x-8'
      : 'opacity-100 translate-x-0'
  }`}
>
  {screens[step]}
</div>
```

---

## Пропуск онбординга

```typescript
const skipOnboarding = async () => {
  if (!user) return;
  
  await supabase
    .from('profiles')
    .update({ onboarding_completed: true })
    .eq('id', user.id);
  
  navigate('/dashboard');
};
```

---

## Завершение онбординга

```typescript
const completeOnboarding = async () => {
  if (!user) return;

  const { error } = await supabase
    .from('profiles')
    .update({
      full_name: data.fullName,
      age: data.age,
      gender: data.gender,
      goals: data.goals,
      tracker_frequency: data.trackerFrequency,
      tracker_times: data.trackerTimes,
      morning_reflection_enabled: data.morningReflectionEnabled,
      morning_reflection_time: data.morningReflectionTime,
      evening_reflection_enabled: data.eveningReflectionEnabled,
      evening_reflection_time: data.eveningReflectionTime,
      onboarding_completed: true,
    })
    .eq('id', user.id);

  if (error) throw error;

  toast({
    title: 'Welcome aboard!',
    description: "You're all set to begin your wellness journey.",
  });

  navigate('/dashboard');
};
```

---

## ProgressIndicator

```typescript
// components/onboarding/ProgressIndicator.tsx
interface ProgressIndicatorProps {
  currentStep: number;
  totalSteps: number;
}

const ProgressIndicator = ({ currentStep, totalSteps }: ProgressIndicatorProps) => (
  <div className="flex justify-center gap-2">
    {Array.from({ length: totalSteps }).map((_, i) => (
      <div
        key={i}
        className={cn(
          'w-2 h-2 rounded-full transition-colors',
          i === currentStep ? 'bg-primary' : 'bg-muted'
        )}
      />
    ))}
  </div>
);
```

---

## Редирект после авторизации

```typescript
// В ProtectedRoute или AuthContext
useEffect(() => {
  if (user && !profile?.onboarding_completed) {
    navigate('/onboarding');
  }
}, [user, profile]);
```
