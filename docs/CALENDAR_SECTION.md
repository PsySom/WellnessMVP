# Раздел Calendar (/calendar)

## Оглавление
1. [Общее описание](#общее-описание)
2. [Структура файлов](#структура-файлов)
3. [Архитектура компонентов](#архитектура-компонентов)
4. [Модели данных](#модели-данных)
5. [Бизнес-логика](#бизнес-логика)
6. [UI/UX описание](#uiux-описание)
7. [Взаимодействие с бэкендом](#взаимодействие-с-бэкендом)
8. [Локализация](#локализация)

---

## Общее описание

Раздел **Calendar** представляет собой центральный модуль управления активностями пользователя. Он позволяет:
- Просматривать активности в виде списка по дням или в виде месячного календаря
- Создавать, редактировать и удалять активности
- Использовать шаблоны активностей для быстрого добавления
- Отмечать выполнение активностей
- Управлять повторяющимися активностями
- Применять предустановленные наборы (пресеты) активностей

---

## Структура файлов

```
src/
├── pages/
│   └── Calendar.tsx                    # Главная страница раздела
├── components/calendar/
│   ├── ListView.tsx                    # Представление списком (по дням)
│   ├── CalendarView.tsx                # Месячное представление календаря
│   ├── TimeSlotSection.tsx             # Секция временного слота (утро, день и т.д.)
│   ├── ActivityItem.tsx                # Карточка отдельной активности
│   ├── ActivityFormModal.tsx           # Модальное окно создания/редактирования
│   ├── ActivityDetailModal.tsx         # Модальное окно просмотра деталей
│   ├── TemplatesSidebar.tsx            # Боковая панель с шаблонами и пресетами
│   ├── TemplateQuickView.tsx           # Попап быстрого просмотра шаблона
│   ├── PresetEditModal.tsx             # Модальное окно редактирования пресета
│   └── DaySection.tsx                  # Секция дня (альтернативная группировка)
├── config/
│   ├── activityPresets.ts              # Системные предустановленные наборы
│   └── categoryConfig.ts               # Конфигурация категорий активностей
├── utils/
│   ├── timeSlots.ts                    # Утилиты для временных слотов
│   ├── recurrenceUtils.ts              # Логика повторения активностей
│   ├── activitySync.ts                 # Синхронизация обновлений активностей
│   └── dateUtils.ts                    # Утилиты для работы с датами
└── types/
    └── preset.ts                       # Типы для пресетов
```

---

## Архитектура компонентов

### Иерархия компонентов

```
Calendar.tsx (Главная страница)
├── AppLayout
├── ResizablePanelGroup
│   ├── ResizablePanel (75% - основной контент)
│   │   ├── Header (навигация, переключатели вида)
│   │   └── Content
│   │       ├── ListView (режим списка)
│   │       │   ├── Day Selector (7 дней недели)
│   │       │   ├── TimeSlotSection × 6 (временные слоты)
│   │       │   │   └── ActivityItem × N
│   │       │   └── Anytime Section
│   │       └── CalendarView (режим месяца)
│   │           └── Day Grid (сетка дней месяца)
│   └── ResizablePanel (25% - сайдбар)
│       └── TemplatesSidebar
│           ├── Preset Buttons
│           ├── Impact Type Filter
│           └── Template Cards
└── ActivityFormModal
```

### Описание ключевых компонентов

#### 1. Calendar.tsx (Главная страница)
**Расположение:** `src/pages/Calendar.tsx`

**Состояние:**
- `view`: `'list' | 'calendar'` — текущий режим отображения
- `currentDate`: `Date` — выбранная дата
- `isAddModalOpen`: `boolean` — состояние модального окна добавления
- `initialSlot`: `string | null` — начальный временной слот для новой активности
- `initialDate`: `string | null` — начальная дата для новой активности

**Функции:**
- `goToPreviousWeek()` / `goToNextWeek()` — навигация по неделям
- `goToToday()` — переход к текущему дню
- `handleSlotClick(slot, date)` — обработка клика по пустому слоту
- `handleModalClose()` — закрытие модального окна

---

#### 2. ListView.tsx (Представление списком)
**Расположение:** `src/components/calendar/ListView.tsx`

**Состояние:**
- `activities`: массив активностей на выбранную дату
- `loading`: состояние загрузки

**Ключевые особенности:**
- Отображает горизонтальный селектор дней недели
- Группирует активности по временным слотам
- Подписывается на realtime-обновления Supabase
- Показывает статистику выполнения внизу экрана

**Временные слоты:**
```typescript
TIME_SLOTS = [
  { key: 'early_morning', startHour: 5, endHour: 9, emoji: '🌅' },   // Раннее утро
  { key: 'late_morning', startHour: 9, endHour: 12, emoji: '☕' },   // Позднее утро
  { key: 'midday', startHour: 12, endHour: 15, emoji: '☀️' },        // Полдень
  { key: 'afternoon', startHour: 15, endHour: 18, emoji: '🌤️' },    // День
  { key: 'evening', startHour: 18, endHour: 22, emoji: '🌆' },       // Вечер
  { key: 'night', startHour: 22, endHour: 5, emoji: '🌙' },          // Ночь
  'anytime' — без привязки ко времени
]
```

---

#### 3. CalendarView.tsx (Месячное представление)
**Расположение:** `src/components/calendar/CalendarView.tsx`

**Ключевые особенности:**
- Отображает сетку дней текущего месяца
- Показывает цветные точки для активностей (по типу воздействия)
- Отображает круговой индикатор прогресса выполнения
- При клике на день переключается в режим списка

**Цветовая индикация типов воздействия:**
```typescript
IMPACT_COLORS = {
  restoring: 'bg-green-500',   // Восстанавливающие
  depleting: 'bg-red-500',     // Истощающие
  neutral: 'bg-orange-500',    // Нейтральные
  mixed: 'bg-blue-500'         // Смешанные
}
```

---

#### 4. TimeSlotSection.tsx (Секция временного слота)
**Расположение:** `src/components/calendar/TimeSlotSection.tsx`

**Props:**
```typescript
interface TimeSlotSectionProps {
  title: string;              // Название слота
  timeRange?: string;         // Диапазон времени (опционально)
  emoji: string;              // Эмодзи слота
  slot: TimeSlot;             // Ключ слота
  activities: any[];          // Активности в этом слоте
  onUpdate: () => void;       // Callback обновления
  date?: string;              // Дата (YYYY-MM-DD)
  onEmptyClick?: (slot) => void; // Клик по пустому слоту
}
```

**Drag & Drop логика:**
- Поддержка перетаскивания шаблонов для создания новых активностей
- Поддержка перемещения существующих активностей между слотами
- Автоматический расчёт нового времени при drop
- Визуальная индикация зоны drop

---

#### 5. ActivityItem.tsx (Карточка активности)
**Расположение:** `src/components/calendar/ActivityItem.tsx`

**Отображаемая информация:**
- Эмодзи и название активности
- Время начала (если указано)
- Длительность в минутах
- Индикатор типа воздействия (цветная точка)
- Звёздочки приоритета (1-5)
- Счётчик повторений (X/Y) для рекуррентных активностей

**Функциональность:**
- Checkbox для отметки выполнения (с confetti-эффектом)
- Раскрывающееся описание
- Кнопка запуска упражнения/теста (если связаны)
- Drag & Drop для перемещения
- Открытие детального просмотра по клику

---

#### 6. ActivityFormModal.tsx (Форма создания/редактирования)
**Расположение:** `src/components/calendar/ActivityFormModal.tsx`

**Поля формы:**
- **Заголовок** — название активности
- **Описание** — опциональное описание
- **Дата** — выбор через календарь
- **Временной слот** — выбор из предустановленных или точное время
- **Длительность** — в минутах (5-1440)
- **Тип воздействия** — restoring/depleting/mixed/neutral
- **Категория** — зависит от типа воздействия
- **Приоритет** — 1-5 звёзд
- **Эмодзи** — эмодзи активности
- **Напоминание** — включение/выключение и время до события
- **Повторение** — настройка рекуррентности

**Типы повторения:**
```typescript
recurrence_type: 'none' | 'daily' | 'weekly' | 'monthly' | 'custom'
```

**Кастомное повторение:**
- Интервал (каждые N дней/недель/месяцев/лет)
- Условие окончания: никогда / до даты / после N повторений

---

#### 7. TemplatesSidebar.tsx (Боковая панель шаблонов)
**Расположение:** `src/components/calendar/TemplatesSidebar.tsx`

**Секции:**
1. **Мои пресеты** — пользовательские наборы активностей
   - Кнопка создания нового пресета
   - Список существующих пресетов с управлением (активация/деактивация/удаление)
   
2. **Системные пресеты** — предустановленные наборы
   - Basic Needs (базовые потребности)
   - Routines (рутины)
   - Development (развитие)
   - Rest (отдых)
   - Other (прочее)

3. **Фильтр по типу воздействия** — all/restoring/depleting/neutral/mixed

4. **Список шаблонов** — карточки activity_templates
   - Drag & Drop для добавления в расписание
   - Клик для просмотра деталей

---

#### 8. PresetEditModal.tsx (Редактор пресетов)
**Расположение:** `src/components/calendar/PresetEditModal.tsx`

**Функциональность:**
- Создание/редактирование пользовательских пресетов
- Drag & Drop шаблонов в части дня
- Настройка длительности и количества повторений каждой активности
- Настройка рекуррентности всего пресета
- Удаление и архивирование пресетов

**Части дня для группировки:**
```typescript
DAY_PARTS = ['early_morning', 'late_morning', 'midday', 'afternoon', 'evening', 'night']
```

---

## Модели данных

### Activity (Активность)
**Таблица:** `activities`

```typescript
interface Activity {
  id: string;
  user_id: string;
  title: string;
  description: string | null;
  date: string;                    // YYYY-MM-DD
  start_time: string | null;       // HH:MM
  end_time: string | null;
  duration_minutes: number | null;
  category: ActivityCategory;       // Enum категорий
  impact_type: ActivityImpact;      // 'restoring' | 'depleting' | 'mixed' | 'neutral'
  status: ActivityStatus;           // 'planned' | 'in_progress' | 'completed' | 'cancelled'
  priority: number | null;          // 1-5
  emoji: string | null;
  is_recurring: boolean | null;
  recurrence_pattern: Json | null;
  repetition_config: Json | null;   // Детальная конфигурация повторений
  reminder_enabled: boolean | null;
  reminder_minutes_before: number | null;
  exercise_id: string | null;       // Связь с упражнением
  test_id: string | null;           // Связь с тестом
  template_id: string | null;       // Связь с шаблоном
  user_preset_id: string | null;    // Связь с пресетом пользователя
  created_at: string;
  updated_at: string;
}
```

### Activity Template (Шаблон активности)
**Таблица:** `activity_templates`

```typescript
interface ActivityTemplate {
  id: string;
  name: string;
  name_en: string;
  name_ru: string | null;
  name_fr: string;
  description: string | null;
  description_ru: string | null;
  category: ActivityCategory;
  impact_type: ActivityImpact;
  default_duration_minutes: number | null;
  emoji: string;
  is_system: boolean | null;
  created_at: string;
}
```

### User Preset (Пресет пользователя)
**Таблица:** `user_presets`

```typescript
interface UserPreset {
  id: string;
  user_id: string;
  name: string;
  emoji: string;
  activities: PresetActivity[];     // JSON массив активностей
  tags: string[] | null;
  is_active: boolean | null;
  is_archived: boolean | null;
  last_activated_at: string | null;
  activation_start_date: string | null;
  activation_end_date: string | null;
  recurrence_type: string | null;
  recurrence_count: number | null;
  custom_interval: number | null;
  custom_unit: string | null;
  custom_end_type: string | null;
  custom_end_date: string | null;
  custom_end_count: number | null;
  created_at: string;
  updated_at: string;
}

interface PresetActivity {
  template_id: string;
  category: string;
  day_part: string;                 // Часть дня для размещения
  duration: number;
  repetitions: number;              // Количество раз в день
}
```

### Категории активностей (ActivityCategory)
```typescript
type ActivityCategory = 
  | 'sleep' | 'nutrition' | 'hydration' | 'exercise' | 'leisure'
  | 'hobby' | 'work' | 'social' | 'practice' | 'health'
  | 'reflection' | 'rest' | 'psychological_exercises'
  | 'light_exercise' | 'close_socializing' | 'walks' | 'games'
  | 'creative' | 'entertainment' | 'self_care' | 'meditation'
  | 'nature' | 'study' | 'commute' | 'intense_exercise'
  | 'household_chores' | 'tasks' | 'finances' | 'health_appointments'
  // ... и многие другие специализированные категории
```

---

## Бизнес-логика

### Создание активности

1. Пользователь нажимает "+" или кликает на пустой слот
2. Открывается `ActivityFormModal`
3. Заполняются поля формы
4. При выборе типа воздействия фильтруются доступные категории
5. При выборе категории автоматически подставляется рекомендуемая длительность
6. Если выбран режим повторения — генерируются все экземпляры активности
7. Активности сохраняются в БД
8. Триггерится обновление UI через `triggerActivityUpdate()`

### Повторяющиеся активности

**Типы повторения:**
- **daily** — ежедневно, N повторений
- **weekly** — еженедельно, N повторений
- **monthly** — ежемесячно, N повторений
- **custom** — кастомный интервал с условием окончания

**Хранение группы повторений:**
```typescript
repetition_config: {
  recurrence_type: 'weekly',
  recurrence_count: 4,
  recurrence_group_id: 'uuid',    // Уникальный ID группы
  custom_interval: 1,
  custom_unit: 'week',
  custom_end_type: 'count',
  custom_end_date: null,
  custom_end_count: 4
}
```

### Редактирование повторяющейся активности

При редактировании рекуррентной активности появляется диалог:
- **Только эту** — изменяется только выбранная активность
- **Все в серии** — изменяются все связанные активности (кроме даты/времени)

### Drag & Drop

**Перетаскивание шаблона в слот:**
1. Начало drag с `templateId` и `templateData`
2. Drop в `TimeSlotSection`
3. Создание новой активности с автоматическим расчётом времени
4. Поддержка множественных повторений в один день

**Перемещение существующей активности:**
1. Начало drag с `activityId`
2. Drop в другой слот или после конкретной активности
3. Обновление `start_time` в БД

### Активация пресета

1. Пользователь нажимает "Play" на пресете
2. Генерируются даты по настройкам рекуррентности
3. Для каждой даты создаются все активности из пресета
4. Обновляется статус пресета (`is_active`, `activation_start_date`, `activation_end_date`)
5. Активности связываются с пресетом через `user_preset_id`

### Деактивация пресета

1. Удаляются все активности с этим `user_preset_id`
2. Сбрасывается статус пресета

---

## UI/UX описание

### Главный экран (/calendar)

```
┌─────────────────────────────────────────────────────────────────┐
│  ← Jan 1 - 7 →                                        [Today]  │
│  2026                                                          │
│                                                                 │
│  ┌──────────────┐                                              │
│  │ List │ Calendar │        [+ Add Activity]                   │
│  └──────────────┘                                              │
├─────────────────────────────────────────────────────┬──────────┤
│                                                     │          │
│  ┌─Mon─┐┌─Tue─┐┌─Wed─┐┌─Thu─┐┌─Fri─┐┌─Sat─┐┌─Sun─┐ │ Templates │
│  │  1  ││  2  ││  3  ││  4  ││  5  ││  6  ││  7  │ │          │
│  └─────┘└─────┘└─────┘└─────┘└─────┘└─────┘└─────┘ │ My Presets│
│                                                     │ ├─ 🍎 Diet │
│  🌅 Early Morning (05:00-09:00)          0/0       │ ├─ 🏃 Sport│
│  ┌─────────────────────────────────────────────┐   │          │
│  │ Click to add or drag template               │   │ System   │
│  └─────────────────────────────────────────────┘   │ ├─ 🔋 Basic│
│                                                     │ ├─ 🔄 Rout.│
│  ☕ Late Morning (09:00-12:00)            2/3       │          │
│  ┌─────────────────────────────────────────────┐   │ Filter   │
│  │ ☑ 🧘 Morning Yoga              ⭐⭐⭐ 08:00  30m │ │ ○ All    │
│  │ ☐ 📚 Study Session             ⭐⭐⭐ 10:00  60m │ │ ○ Restor.│
│  │ ☑ ☕ Coffee Break              ⭐⭐   11:00  15m │ │ ○ Depl.  │
│  └─────────────────────────────────────────────┘   │          │
│                                                     │ Templates│
│  ☀️ Midday (12:00-15:00)                  1/1       │ ├─ 🧘 Yoga │
│  ...                                                │ ├─ 📚 Study│
│                                                     │ ├─ 🏃 Run  │
│  ──────────────────────────────────────────────    │          │
│  2/6 completed (33%)           🎉 Great progress!  │          │
└─────────────────────────────────────────────────────┴──────────┘
```

### Карточка активности

```
┌──────────────────────────────────────────────────────────────┐
│ │ ☐  🧘 Morning Yoga Session      ⭐⭐⭐           [▼]      │
│ │                                                            │
│ │      ● 08:00    │ 30m │  ● (green)  │ 🔄 3/7              │
│ │                                                            │
│ │      (Description expands on click)                        │
└──────────────────────────────────────────────────────────────┘

Легенда:
│  - Цветная полоса статуса (green = completed, primary = planned)
☐  - Checkbox выполнения
🧘 - Эмодзи активности
⭐⭐⭐ - Приоритет (3 из 5)
● 08:00 - Время начала с индикатором
30m - Длительность
● (green) - Цветной индикатор типа воздействия
🔄 3/7 - Счётчик рекуррентности (3 оставшихся из 7 всего)
[▼] - Кнопка раскрытия описания
```

### Модальное окно создания активности

```
┌─────────────────────────────────────────────────────────────┐
│  ✕                    New Activity                          │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  Impact Type:  [Restoring] [Depleting] [Mixed] [Neutral]   │
│                                                             │
│  Category:     [Sleep ▼]                                    │
│                                                             │
│  Title:        [Morning Yoga Session                    ]   │
│                                                             │
│  Description:  [Optional description...                 ]   │
│                                                             │
│  Date:         [📅 Jan 3, 2026                          ]   │
│                                                             │
│  Time Slot:    [Late Morning (09:00-12:00) ▼]              │
│  -or- Exact:   [09:00]                                      │
│                                                             │
│  Duration:     [30] minutes        (- 5 +)                  │
│                                                             │
│  Priority:     ⭐⭐⭐☆☆                                      │
│                                                             │
│  Emoji:        [🧘]                                         │
│                                                             │
│  ┌─ Reminder ─────────────────────────────────────────┐    │
│  │ [✓] Enable reminder    [15] minutes before          │    │
│  └─────────────────────────────────────────────────────┘    │
│                                                             │
│  ┌─ Recurrence ───────────────────────────────────────┐    │
│  │ [None ▼] [Daily] [Weekly] [Monthly] [Custom]       │    │
│  │                                                     │    │
│  │ Repeat [7] times                                    │    │
│  └─────────────────────────────────────────────────────┘    │
│                                                             │
├─────────────────────────────────────────────────────────────┤
│                              [Cancel]  [Save Activity]      │
└─────────────────────────────────────────────────────────────┘
```

### Месячное представление (CalendarView)

```
┌─────────────────────────────────────────────────────────────┐
│                      January 2026                            │
├────────┬────────┬────────┬────────┬────────┬────────┬───────┤
│  Mon   │  Tue   │  Wed   │  Thu   │  Fri   │  Sat   │  Sun  │
├────────┼────────┼────────┼────────┼────────┼────────┼───────┤
│   29   │   30   │   31   │    1   │    2   │   [3]  │    4  │
│        │        │        │ ●●     │ ●●●    │ ●●●●   │       │
│        │        │        │ (75%)  │ (50%)  │ (25%)  │       │
├────────┼────────┼────────┼────────┼────────┼────────┼───────┤
│    5   │    6   │    7   │    8   │    9   │   10   │   11  │
│ ●      │        │ ●●     │        │ ●●●    │        │       │
│ (100%) │        │ (66%)  │        │ (33%)  │        │       │
└────────┴────────┴────────┴────────┴────────┴────────┴───────┘

Легенда:
● - Цветные точки активностей (зелёные/красные/синие/оранжевые)
(75%) - Круговой индикатор процента выполнения
[3] - Выбранный день (подсвечен)
```

---

## Взаимодействие с бэкендом

### Запросы к Supabase

**Получение активностей на дату:**
```typescript
supabase
  .from('activities')
  .select('*, exercises(slug), tests(slug)')
  .eq('user_id', user.id)
  .eq('date', 'YYYY-MM-DD')
  .order('start_time', { ascending: true })
```

**Получение шаблонов:**
```typescript
supabase
  .from('activity_templates')
  .select('*')
  .order('category', { ascending: true })
  .order('name_en', { ascending: true })
```

**Создание активности:**
```typescript
supabase
  .from('activities')
  .insert(activityData)
```

**Обновление статуса:**
```typescript
supabase
  .from('activities')
  .update({ status: 'completed' })
  .eq('id', activityId)
```

### Realtime подписки

```typescript
const channel = supabase
  .channel('activities-realtime-listview')
  .on(
    'postgres_changes',
    {
      event: '*',
      schema: 'public',
      table: 'activities',
      filter: `user_id=eq.${user.id}`
    },
    (payload) => {
      fetchActivities();
    }
  )
  .subscribe();
```

### Синхронизация обновлений

Для обновления всех компонентов при изменении данных используется:

```typescript
// activitySync.ts
export const triggerActivityUpdate = () => {
  window.dispatchEvent(new Event('activity-updated'));
};

// В компонентах:
window.addEventListener('activity-updated', fetchActivities);
```

---

## Локализация

### Ключи переводов

**Файлы:** `src/locales/{en,ru,fr}/translation.json`

```json
{
  "calendar": {
    "today": "Today",
    "list": "List",
    "calendar": "Calendar",
    "addActivity": "Add Activity",
    "completed": "completed",
    "greatProgress": "Great progress!",
    
    "sections": {
      "early_morning": "Early Morning",
      "late_morning": "Late Morning",
      "midday": "Midday",
      "afternoon": "Afternoon",
      "evening": "Evening",
      "night": "Night",
      "anytime": "Anytime"
    },
    
    "timeRanges": {
      "early_morning": "05:00-09:00",
      "late_morning": "09:00-12:00",
      // ...
    },
    
    "weekdays": {
      "short": {
        "mon": "Mon",
        "tue": "Tue",
        // ...
      }
    },
    
    "form": {
      "title": "Title",
      "description": "Description",
      "date": "Date",
      "time": "Time",
      "duration": "Duration",
      "category": "Category",
      "priority": "Priority",
      "saveError": "Failed to save activity",
      "updateSuccess": "Activity updated",
      // ...
    },
    
    "presets": {
      "addAll": "Add all",
      "addCore": "Add core",
      "activitiesAdded": "Added {{count}} activities",
      // ...
    }
  }
}
```

### Локализация категорий

Категории используют конфигурацию из `categoryConfig.ts`:

```typescript
{
  value: 'meditation',
  emoji: '🧘',
  recommendedType: 'restoring',
  defaultDuration: 20,
  label: {
    en: 'Meditation',
    ru: 'Медитация',
    fr: 'Méditation'
  }
}
```

---

## Система активностей

### Общая концепция

Активность — это единица планирования времени пользователя. Каждая активность характеризуется:
- **Названием и описанием**
- **Типом воздействия** (влияние на энергию)
- **Категорией** (тематическая классификация)
- **Временными параметрами** (дата, время, длительность)
- **Приоритетом** (важность)
- **Статусом выполнения**

---

### Типы воздействия (Impact Types)

Каждая активность имеет один из четырёх типов воздействия на энергетический баланс пользователя:

| Тип | Emoji | Цвет | Описание |
|-----|-------|------|----------|
| **Restoring** (Восстанавливающие) | 🟢 | `green-500` | Помогают восстановить энергию, снизить стресс, улучшить самочувствие |
| **Depleting** (Истощающие) | 🔴 | `red-500` | Требуют затрат энергии, умственного или физического напряжения |
| **Mixed** (Смешанные) | 🔵 | `blue-500` | Сложные, но приносящие удовлетворение. Могут как истощать, так и восстанавливать |
| **Neutral** (Нейтральные) | 🟠 | `orange-500` | Не оказывают существенного влияния на энергию |

**Цветовая кодировка в коде:**
```typescript
const IMPACT_COLORS = {
  restoring: 'bg-green-500',
  depleting: 'bg-red-500',
  mixed: 'bg-blue-500',
  neutral: 'bg-orange-500'
};
```

---

### Полный перечень категорий активностей

#### 1. RESTORING (Восстанавливающие) — 29 категорий

**Группа: Сон (Sleep)**
| Категория | Emoji | Длительность по умолчанию | Описание |
|-----------|-------|---------------------------|----------|
| `sleep_8hours` | 😴 | 480 мин | Полноценный 8-часовой сон |
| `sleep_nap` | 😴 | 60 мин | Дневной сон (30 мин - 2 часа) |
| `sleep_quiet_rest` | 🛋️ | — | Спокойный отдых |

**Группа: Питание (Nutrition)**
| Категория | Emoji | Описание |
|-----------|-------|----------|
| `nutrition_breakfast` | 🍳 | Завтрак |
| `nutrition_brunch` | 🥐 | Бранч (поздний завтрак) |
| `nutrition_lunch` | 🍽️ | Обед |
| `nutrition_coffee` | ☕ | Кофе |
| `nutrition_dinner` | 🍲 | Ужин |
| `nutrition_after_dinner` | 🍪 | Еда после ужина |

**Группа: Гидратация (Hydration)**
| Категория | Emoji | Описание |
|-----------|-------|----------|
| `hydration` | 💧 | Питьё воды |

**Группа: Отдых и практики (Rest & Practices)**
| Категория | Emoji | Длительность | Описание |
|-----------|-------|--------------|----------|
| `rest` | 🛋️ | — | Отдых |
| `rest_relaxation` | 😌 | — | Расслабление |
| `rest_water_procedures` | 🛁 | — | Водные процедуры |
| `rest_hygiene` | 🚿 | — | Гигиена |
| `rest_self_care` | 💆 | — | Процедуры ухода за собой |
| `rest_meditation_10min` | 🧘‍♂️ | 10 мин | Медитация |
| `rest_psychological_exercises` | 🧠 | — | Психологические упражнения |
| `rest_walks` | 🚶 | — | Прогулки |
| `rest_light_exercise` | 🧘 | — | Лёгкий спорт |
| `rest_morning_exercise` | 🤸 | 10 мин | Утренняя зарядка |
| `rest_reading` | 📖 | — | Чтение |
| `rest_doing_nothing` | 🪑 | — | Бездействие |
| `rest_breathing_5min` | 🌬️ | 5 мин | Дыхательное упражнение |
| `rest_grounding_10min` | 🌍 | 10 мин | Упражнение на заземление |

**Группа: Общение, хобби и развлечения (Social & Entertainment)**
| Категория | Emoji | Описание |
|-----------|-------|----------|
| `close_socializing` | 💝 | Общение с близкими |
| `hobby` | 🎨 | Хобби |
| `games` | 🎮 | Игры |
| `creative` | 🎭 | Творчество |
| `entertainment` | 🎬 | Развлечения |
| `self_care` | 💆 | Забота о себе |
| `entertainment_music` | 🎵 | Прослушивание музыки |
| `entertainment_social_media` | 📱 | Соцсети |
| `entertainment_movies` | 🎬 | Просмотр фильмов |

---

#### 2. DEPLETING (Истощающие) — 12 категорий

| Категория | Emoji | Описание |
|-----------|-------|----------|
| `work` | 💼 | Работа |
| `study` | 📚 | Учёба |
| `commute` | 🚗 | Дорога/поездка |
| `intense_exercise` | 🏋️ | Интенсивный спорт |
| `household_chores` | 🧹 | Домашние дела |
| `tasks` | ✅ | Задачи |
| `finances` | 💰 | Финансы |
| `health_appointments` | 🏥 | Визиты к врачу |
| `caregiving` | 👶 | Забота о других |
| `learning` | 📖 | Обучение |
| `problem_solving` | 🧩 | Решение проблем |
| `social_obligations` | 👔 | Социальные обязательства |

---

#### 3. MIXED (Смешанные) — 11 категорий

| Категория | Emoji | Описание |
|-----------|-------|----------|
| `exercise` | 🏃 | Спорт |
| `moderate_exercise` | 🚴 | Умеренный спорт |
| `social` | 👥 | Общение |
| `cooking` | 🍳 | Готовка |
| `projects` | 🎯 | Проекты |
| `shopping` | 🛒 | Покупки |
| `cleaning` | 🧽 | Наведение порядка |
| `planning` | 📅 | Планирование |
| `reading` | 📚 | Чтение |
| `volunteering` | 🤝 | Волонтёрство |
| `spiritual` | 🕉️ | Духовные практики |

---

#### 4. NEUTRAL (Нейтральные) — 10 категорий

| Категория | Emoji | Длительность | Описание |
|-----------|-------|--------------|----------|
| `hygiene` | 🚿 | — | Гигиена |
| `waiting` | ⏳ | — | Ожидание |
| `testing` | 📊 | — | Тестирование |
| `practice` | 🧘 | — | Практика |
| `health` | 🩺 | — | Здоровье |
| `reflection` | 📝 | — | Рефлексия |
| `reflection_trackers_5min` | 📊 | 5 мин | Заполнение трекеров |
| `reflection_evening_10min` | 🌙 | 10 мин | Вечерняя рефлексия |
| `reflection_morning_10min` | 🌅 | 10 мин | Утренняя рефлексия |
| `other` | ⚡ | — | Другое |

---

### Временные слоты

Активности группируются по временным слотам для удобного планирования дня:

| Слот | Ключ | Emoji | Часы | Описание |
|------|------|-------|------|----------|
| Раннее утро | `early_morning` | 🌅 | 05:00-09:00 | Время пробуждения и утренних ритуалов |
| Позднее утро | `late_morning` | ☕ | 09:00-12:00 | Активное утро, начало рабочего дня |
| Полдень | `midday` | ☀️ | 12:00-15:00 | Обед и ранний полдень |
| День | `afternoon` | 🌤️ | 15:00-18:00 | Послеобеденное время |
| Вечер | `evening` | 🌆 | 18:00-22:00 | Вечерний отдых и дела |
| Ночь | `night` | 🌙 | 22:00-05:00 | Ночное время и сон |
| В любое время | `anytime` | 📌 | — | Без привязки ко времени |

**Определение слота по времени:**
```typescript
export const getTimeSlotForTime = (time: string | null): TimeSlot => {
  if (!time) return 'anytime';
  const [hours] = time.split(':').map(Number);
  
  for (const slot of TIME_SLOTS) {
    if (slot.key === 'night') {
      if (hours >= slot.startHour || hours < slot.endHour) return slot.key;
    } else {
      if (hours >= slot.startHour && hours < slot.endHour) return slot.key;
    }
  }
  return 'anytime';
};
```

---

## Логика создания активностей

### Способы создания

1. **Через модальное окно**
   - Нажатие кнопки "+" в хедере календаря
   - Открывается `ActivityFormModal` с пустыми полями

2. **Через клик на пустой слот**
   - Клик на пустую область в `TimeSlotSection`
   - Открывается `ActivityFormModal` с предзаполненными датой и слотом

3. **Через Drag & Drop шаблона**
   - Перетаскивание карточки шаблона из сайдбара
   - Drop в нужный временной слот
   - Автоматическое создание активности с данными из шаблона

4. **Через шаблон с кнопки**
   - Клик на "+" в карточке шаблона (`TemplateCard`)
   - Быстрое добавление в текущий день

5. **Через активацию пресета**
   - Нажатие "Play" на пресете
   - Массовое создание активностей по расписанию

### Алгоритм создания (ActivityFormModal)

```typescript
// 1. Валидация данных формы
activitySchema.parse({
  title: formData.title,
  description: formData.description,
  duration_minutes: formData.duration_minutes,
});

// 2. Определение времени начала
let startTime = null;
if (formData.timeSlot === 'exact_time') {
  startTime = formData.start_time;
} else if (formData.timeSlot !== 'anytime') {
  startTime = getDefaultTimeForSlot(formData.timeSlot);
}

// 3. Формирование объекта активности
const activityData = {
  title: formData.title,
  description: formData.description,
  date: format(formData.date, 'yyyy-MM-dd'),
  start_time: startTime,
  duration_minutes: formData.duration_minutes,
  impact_type: formData.impact_type,
  category: formData.category,
  priority: formData.priority,
  is_recurring: formData.recurrence_type !== 'none',
  emoji: formData.emoji,
  user_id: user.id,
  repetition_config: {
    recurrence_type: formData.recurrence_type,
    recurrence_count: formData.recurrence_count,
    recurrence_group_id: formData.recurrence_type !== 'none' ? crypto.randomUUID() : null,
    // ...
  }
};

// 4. Проверка на дубликаты
const { data: existing } = await supabase
  .from('activities')
  .select('*')
  .eq('user_id', user.id)
  .eq('date', formData.date)
  .eq('category', formData.category);

if (existing?.length > 0) {
  // Показать диалог подтверждения
}

// 5. Генерация дат для повторяющихся активностей
const dates = generateRecurrenceDates();

// 6. Массовая вставка в БД
await supabase.from('activities').insert(activitiesToCreate);

// 7. Уведомление об успехе и обновление UI
toast.success('Activity created');
triggerActivityUpdate();
```

### Проверка на дубликаты

При создании активности система проверяет наличие активности с такой же категорией на ту же дату. Если найдена — показывается диалог подтверждения:

```
┌─────────────────────────────────────────────┐
│  ⚠️ Similar activity exists                  │
│                                             │
│  You already have an activity with this     │
│  category scheduled for this day.           │
│                                             │
│           [Cancel]  [Add anyway]            │
└─────────────────────────────────────────────┘
```

---

## Логика редактирования активностей

### Открытие редактирования

1. Клик на карточку активности → открывается `ActivityDetailModal`
2. Нажатие "Edit" → открывается `ActivityFormModal` с заполненными данными

### Редактирование одиночной активности

- Изменяются все поля формы
- При сохранении обновляется запись в БД
- Триггерится обновление UI

### Редактирование повторяющейся активности

При редактировании активности из рекуррентной серии или пресета появляется диалог выбора:

```
┌─────────────────────────────────────────────┐
│  Edit Recurring Activity                     │
│                                             │
│  This activity is part of a recurring       │
│  series. What would you like to edit?       │
│                                             │
│  ○ Only this occurrence                     │
│  ○ All occurrences in the series            │
│                                             │
│           [Cancel]  [Save]                  │
└─────────────────────────────────────────────┘
```

**"Only this occurrence"** — редактируется только выбранная активность

**"All occurrences"** — редактируются все связанные активности (кроме даты и времени):
```typescript
// Обновляемые поля для всей серии
const updateData = {
  title: formData.title,
  description: formData.description,
  duration_minutes: formData.duration_minutes,
  impact_type: formData.impact_type,
  category: formData.category,
  priority: formData.priority,
  reminder_enabled: formData.reminder_enabled,
  emoji: formData.emoji,
};

// Поиск всех активностей в группе
const activityIds = allActivities
  .filter(a => a.repetition_config?.recurrence_group_id === groupId)
  .map(a => a.id);

await supabase.from('activities').update(updateData).in('id', activityIds);
```

---

## Логика удаления активностей

### Удаление одиночной активности

1. Открыть `ActivityDetailModal`
2. Нажать "Delete"
3. Подтвердить в диалоге
4. Активность удаляется из БД

### Удаление повторяющейся активности

Аналогично редактированию, показывается диалог выбора:

```
┌─────────────────────────────────────────────┐
│  Delete Activity                             │
│                                             │
│  This activity is part of a recurring       │
│  series. What would you like to delete?     │
│                                             │
│  ○ Only this occurrence                     │
│  ○ All occurrences (X activities)           │
│                                             │
│           [Cancel]  [Delete]                │
└─────────────────────────────────────────────┘
```

**Код удаления всей серии:**
```typescript
if (deleteMode === 'all') {
  const activityIds = allActivities
    .filter(a => {
      if (recurrenceGroupId) {
        return a.repetition_config?.recurrence_group_id === recurrenceGroupId;
      }
      if (userPresetId) {
        return a.user_preset_id === userPresetId;
      }
      return false;
    })
    .map(a => a.id);

  await supabase.from('activities').delete().in('id', activityIds);
}
```

---

## Логика отслеживания выполнения

### Статусы активности

| Статус | Описание |
|--------|----------|
| `planned` | Запланирована, не выполнена |
| `in_progress` | В процессе выполнения (не используется активно) |
| `completed` | Выполнена |
| `cancelled` | Отменена (не используется активно) |

### Отметка выполнения

В `ActivityItem` реализован checkbox для быстрой отметки:

```typescript
const handleToggleComplete = async () => {
  const newStatus = activity.status === 'completed' ? 'planned' : 'completed';
  
  const { error } = await supabase
    .from('activities')
    .update({ status: newStatus })
    .eq('id', activity.id);

  if (!error && newStatus === 'completed') {
    // Запуск confetti-эффекта при выполнении
    confetti({
      particleCount: 100,
      spread: 70,
      origin: { y: 0.6 }
    });
  }
  
  onUpdate();
};
```

### Статистика выполнения

На экране календаря (ListView) показывается общая статистика:

```
─────────────────────────────────────────────
  2/6 completed (33%)           🎉 Great progress!
```

**Расчёт в CalendarView:**
```typescript
const getCompletionRate = (dayActivities: any[]) => {
  if (dayActivities.length === 0) return 0;
  const completed = dayActivities.filter(a => a.status === 'completed').length;
  return Math.round((completed / dayActivities.length) * 100);
};
```

### Визуализация в месячном календаре

Каждый день отображает:
1. **Цветные точки** — по одной на каждую активность (цвет = тип воздействия)
2. **Круговой индикатор** — процент выполнения дня

```
│    3   │    4   │    5   │
│ ●●●●   │ ●●     │ ●      │
│ (75%)  │ (50%)  │ (100%) │
```

---

## Отображение активностей в Dashboard

### Компонент TodayActivitiesCard

Отображает активности текущего дня на главной странице:

```typescript
const TodayActivitiesCard = ({ activities, onUpdate }) => {
  return (
    <Card>
      <h2>📅 Today's Activities</h2>
      <p>{today}</p>
      
      {TIME_SLOTS.map((slot) => (
        <TimeSlotSection
          key={slot.key}
          activities={filterActivitiesBySlot(activities, slot.key)}
          onUpdate={onUpdate}
        />
      ))}
      
      <TimeSlotSection
        slot="anytime"
        activities={filterActivitiesBySlot(activities, 'anytime')}
      />
      
      <footer>
        {completed}/{total} completed
      </footer>
    </Card>
  );
};
```

### Компонент ActivityImpactCards

Показывает круговые диаграммы по типам воздействия:

```
┌─────────────────────────────────────────────┐
│  [🟢 75%]    [🔴 50%]    [🔵 33%]    [🟠 0%] │
│  Restoring   Depleting   Mixed      Neutral │
│  3/4         1/2         1/3         0/0    │
└─────────────────────────────────────────────┘
```

---

## Система шаблонов активностей

### Что такое шаблон

**Activity Template** — это предопределённый образец активности, хранящийся в таблице `activity_templates`. Шаблоны:
- Создаются администраторами системы (`is_system = true`)
- Содержат базовую информацию (название, категорию, тип, длительность)
- Локализованы на 3 языка (EN, RU, FR)
- Доступны всем пользователям для быстрого добавления активностей

### Структура шаблона

```typescript
interface ActivityTemplate {
  id: string;                          // UUID
  name: string;                        // Системное имя
  name_en: string;                     // Название (EN)
  name_ru: string | null;              // Название (RU)
  name_fr: string;                     // Название (FR)
  description: string | null;          // Описание
  description_ru: string | null;       // Описание (RU)
  category: ActivityCategory;          // Категория
  impact_type: ActivityImpact;         // Тип воздействия
  default_duration_minutes: number;    // Длительность по умолчанию
  emoji: string;                       // Эмодзи
  is_system: boolean;                  // Системный шаблон
  created_at: string;                  // Дата создания
}
```

### Отображение шаблонов

Шаблоны отображаются в `TemplatesSidebar` с фильтрацией по типу воздействия:

```
Templates
├─ Filter: [All] [Restoring] [Depleting] [Mixed] [Neutral]
│
├─ 🧘 Morning Yoga      [+]
│   └─ Restoring • 30m
├─ 📚 Study Session     [+]
│   └─ Depleting • 60m
├─ 🏃 Running           [+]
│   └─ Mixed • 45m
└─ ...
```

### Добавление шаблона в расписание

**Способ 1: Клик на "+"]**
```typescript
// TemplateCard.tsx
const handleAddClick = () => {
  onClick(template);
};
// → Открывается TemplateDetailModal → кнопка "Add to today"
```

**Способ 2: Drag & Drop**
```typescript
// TemplateCard.tsx
const handleDragStart = (e: React.DragEvent) => {
  e.dataTransfer.setData('templateId', template.id);
  e.dataTransfer.setData('templateData', JSON.stringify(template));
};

// TimeSlotSection.tsx
const handleDrop = async (e: React.DragEvent) => {
  const templateData = JSON.parse(e.dataTransfer.getData('templateData'));
  
  await supabase.from('activities').insert({
    title: templateData.name_en,
    category: templateData.category,
    impact_type: templateData.impact_type,
    duration_minutes: templateData.default_duration_minutes,
    date: date,
    start_time: calculateNewTime(),
    user_id: user.id,
    template_id: templateData.id,
    emoji: templateData.emoji,
  });
};
```

### Быстрый просмотр шаблона

При наведении на шаблон появляется `TemplateQuickView` с детальной информацией:

```
┌─────────────────────────────────────┐
│ 🧘 Morning Yoga                     │
│                                     │
│ Start your day with energizing      │
│ yoga practice...                    │
│                                     │
│ Category: Exercise                  │
│ Duration: 30 min                    │
│ Impact: 🟢 Restoring                │
│                                     │
│ [Add to today] [Schedule]           │
└─────────────────────────────────────┘
```

---

## Система пресетов

### Что такое пресет

**User Preset** — это пользовательский набор активностей, сгруппированных для повторяющегося применения. Пресеты позволяют:
- Сохранить комплект активностей для типового дня
- Быстро применить весь набор к выбранным датам
- Настроить автоматическое повторение (рекуррентность)

### Типы пресетов

1. **Пользовательские пресеты** — создаются пользователем в `PresetEditModal`
2. **Системные пресеты** — предопределённые наборы в `activityPresets.ts`:
   - 🔋 Basic Needs (базовые потребности: сон, питание, гидратация)
   - 🔄 Routines (рутины и привычки)
   - 📈 Development (развитие)
   - 🌿 Rest (отдых)
   - ✨ Other (прочее)

### Структура пресета

```typescript
interface UserPreset {
  id: string;
  user_id: string;
  name: string;                    // Название пресета
  emoji: string;                   // Эмодзи
  activities: PresetActivity[];    // Список активностей (JSON)
  tags: string[];                  // Теги для группировки
  
  // Статус
  is_active: boolean;              // Активирован ли
  is_archived: boolean;            // Архивирован ли
  
  // Даты активации
  last_activated_at: string;
  activation_start_date: string;
  activation_end_date: string;
  
  // Настройки рекуррентности
  recurrence_type: string;         // 'none' | 'daily' | 'weekly' | 'monthly' | 'custom'
  recurrence_count: number;        // Количество повторений
  custom_interval: number;         // Интервал для custom
  custom_unit: string;             // 'day' | 'week' | 'month' | 'year'
  custom_end_type: string;         // 'never' | 'date' | 'count'
  custom_end_date: string;
  custom_end_count: number;
}

interface PresetActivity {
  template_id: string;             // ID шаблона активности
  category: string;                // Категория
  day_part: DayPart;               // Часть дня для размещения
  duration: number;                // Длительность в минутах
  repetitions: number;             // Количество раз в день
}
```

### Создание пресета

1. Нажать "+" в секции "My Presets"
2. Открывается `PresetEditModal`
3. Заполнить название и эмодзи
4. Drag & Drop шаблонов в части дня
5. Настроить длительность и повторения для каждой активности
6. Настроить рекуррентность пресета
7. Сохранить

### Активация пресета

При нажатии "Play" на пресете:

```typescript
const handleActivate = async () => {
  // 1. Генерация дат
  const dates = generateRecurrenceDates(new Date(), {
    type: preset.recurrence_type,
    count: preset.recurrence_count,
    // ...
  });

  // 2. Создание активностей для каждой даты
  const activitiesToCreate = [];
  
  for (const date of dates) {
    for (const presetActivity of preset.activities) {
      const template = templates.find(t => t.id === presetActivity.template_id);
      
      for (let i = 0; i < presetActivity.repetitions; i++) {
        activitiesToCreate.push({
          title: template.name,
          category: presetActivity.category,
          impact_type: template.impact_type,
          duration_minutes: presetActivity.duration,
          date: format(date, 'yyyy-MM-dd'),
          start_time: getDefaultTimeForSlot(presetActivity.day_part),
          user_id: user.id,
          user_preset_id: preset.id,  // Связь с пресетом
          template_id: template.id,
          emoji: template.emoji,
        });
      }
    }
  }

  // 3. Массовая вставка
  await supabase.from('activities').insert(activitiesToCreate);

  // 4. Обновление статуса пресета
  await supabase.from('user_presets').update({
    is_active: true,
    last_activated_at: new Date().toISOString(),
    activation_start_date: format(dates[0], 'yyyy-MM-dd'),
    activation_end_date: format(dates[dates.length - 1], 'yyyy-MM-dd'),
  }).eq('id', preset.id);
};
```

### Деактивация пресета

При нажатии "Stop" или удалении пресета:

```typescript
const handleDeactivate = async () => {
  // 1. Удаление всех связанных активностей
  await supabase
    .from('activities')
    .delete()
    .eq('user_preset_id', preset.id);

  // 2. Сброс статуса пресета
  await supabase.from('user_presets').update({
    is_active: false,
    activation_start_date: null,
    activation_end_date: null,
  }).eq('id', preset.id);
};
```

### Теги пресетов

Пресеты можно помечать тегами для организации:

| Тег | Emoji | Описание |
|-----|-------|----------|
| routine | 🔄 | Рутины |
| care | 💆 | Уход за собой |
| health | 🏥 | Здоровье |
| sport | 🏃 | Спорт |
| habits | ✅ | Привычки |
| tasks | 📋 | Задачи |
| rest | 🌿 | Отдых |
| learning | 📚 | Обучение |
| development | 📈 | Развитие |
| antistress | 😌 | Антистресс |
| basic_needs | 🔋 | Базовые потребности |
| other | ⚡ | Другое |

---

## Заключение

Раздел Calendar представляет собой сложную систему управления активностями с поддержкой:
- **62+ категорий активностей** в 4 типах воздействия
- **7 временных слотов** для структурирования дня
- **Двух режимов отображения** (список/календарь)
- **Drag & Drop** для удобного планирования
- **Рекуррентных активностей** с гибкой настройкой
- **Шаблонов** для быстрого добавления типовых активностей
- **Пресетов** для массового применения наборов
- **Отслеживания выполнения** с визуализацией прогресса
- **Realtime синхронизации** между вкладками
- **Полной локализации** на 3 языка (EN, RU, FR)

Архитектура построена на принципах разделения ответственности, где каждый компонент отвечает за свою узкую область функциональности.
