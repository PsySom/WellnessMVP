# Типы и виды активностей и шаблонов

## Общее описание

Система активностей в приложении построена на двухуровневой модели:
1. **Activity Templates** (Шаблоны активностей) — системные шаблоны в БД
2. **Activities** (Активности) — конкретные экземпляры, созданные пользователем

---

## Структура файлов

```
src/
├── config/
│   ├── categoryConfig.ts         # Конфигурация категорий активностей
│   └── activityPresets.ts        # Системные пресеты активностей
├── types/
│   └── preset.ts                 # Типы пользовательских пресетов
├── utils/
│   ├── timeSlots.ts              # Временные слоты
│   └── recurrenceUtils.ts        # Утилиты повторения
└── integrations/supabase/
    └── types.ts                  # Типы БД (Enums)
```

---

## Типы воздействия (Impact Types)

Каждая активность имеет тип воздействия на энергию и состояние пользователя:

| Тип | Значение | Описание | Цветовая индикация |
|-----|----------|----------|-------------------|
| 🟢 Restoring | `restoring` | Восстанавливающие | Зелёный |
| 🔴 Depleting | `depleting` | Истощающие | Красный |
| 🟡 Mixed | `mixed` | Смешанные | Жёлтый/оранжевый |
| ⚪ Neutral | `neutral` | Нейтральные | Серый |

```typescript
export type ImpactType = 'restoring' | 'depleting' | 'mixed' | 'neutral';
```

---

## Категории активностей

### Структура категории

```typescript
interface CategoryConfig {
  value: CategoryKey;           // Ключ категории (enum)
  emoji: string;                // Эмодзи для отображения
  recommendedType: ImpactType;  // Рекомендуемый тип воздействия
  defaultDuration?: number;     // Длительность по умолчанию (мин)
  group?: string;               // Группа для визуального разделения
  label: {
    en: string;
    ru: string;
    fr: string;
  };
}
```

### 1. RESTORING (Восстанавливающие) 🟢

#### Группа: Сон (sleep)
| Категория | Emoji | Длительность | Описание |
|-----------|-------|--------------|----------|
| `sleep_8hours` | 😴 | 480 мин | Сон 8 часов |
| `sleep_nap` | 😴 | 60 мин | Короткий дневной сон |
| `sleep_quiet_rest` | 🛋️ | — | Спокойный отдых |

#### Группа: Питание (nutrition)
| Категория | Emoji | Описание |
|-----------|-------|----------|
| `nutrition_breakfast` | 🍳 | Завтрак |
| `nutrition_brunch` | 🥐 | Бранч |
| `nutrition_lunch` | 🍽️ | Обед |
| `nutrition_coffee` | ☕ | Кофе |
| `nutrition_dinner` | 🍲 | Ужин |
| `nutrition_after_dinner` | 🍪 | Еда после ужина |

#### Группа: Гидратация (hydration)
| Категория | Emoji | Описание |
|-----------|-------|----------|
| `hydration` | 💧 | Гидратация |

#### Группа: Отдых и практики (rest_practices)
| Категория | Emoji | Длительность | Описание |
|-----------|-------|--------------|----------|
| `rest` | 🛋️ | — | Отдых |
| `rest_relaxation` | 😌 | — | Расслабление |
| `rest_water_procedures` | 🛁 | — | Водные процедуры |
| `rest_hygiene` | 🚿 | — | Гигиена |
| `rest_self_care` | 💆 | — | Уход за собой |
| `rest_meditation_10min` | 🧘‍♂️ | 10 мин | Медитация |
| `rest_psychological_exercises` | 🧠 | — | Психологические упражнения |
| `rest_walks` | 🚶 | — | Прогулки |
| `rest_light_exercise` | 🧘 | — | Легкий спорт |
| `rest_morning_exercise` | 🤸 | 10 мин | Утренняя зарядка |
| `rest_reading` | 📖 | — | Чтение |
| `rest_doing_nothing` | 🪑 | — | Бездействие |
| `rest_breathing_5min` | 🌬️ | 5 мин | Дыхательное упражнение |
| `rest_grounding_10min` | 🌍 | 10 мин | Упражнение на заземление |

#### Группа: Общение и развлечения (social_hobbies)
| Категория | Emoji | Описание |
|-----------|-------|----------|
| `close_socializing` | 💝 | Общение с близкими |
| `hobby` | 🎨 | Хобби |
| `games` | 🎮 | Игры |
| `creative` | 🎭 | Творчество |
| `entertainment` | 🎬 | Развлечения |
| `self_care` | 💆 | Забота о себе |
| `entertainment_music` | 🎵 | Послушать музыку |
| `entertainment_social_media` | 📱 | Соцсети |
| `entertainment_movies` | 🎬 | Просмотр фильмов |

---

### 2. DEPLETING (Истощающие) 🔴

| Категория | Emoji | Описание |
|-----------|-------|----------|
| `work` | 💼 | Работа |
| `study` | 📚 | Учеба |
| `commute` | 🚗 | Дорога |
| `intense_exercise` | 🏋️ | Интенсивный спорт |
| `household_chores` | 🧹 | Домашние дела |
| `tasks` | ✅ | Задачи |
| `finances` | 💰 | Финансы |
| `health_appointments` | 🏥 | Медицинские приёмы |
| `caregiving` | 👶 | Забота о других |
| `learning` | 📖 | Обучение |
| `problem_solving` | 🧩 | Решение проблем |
| `social_obligations` | 👔 | Социальные обязательства |

---

### 3. MIXED (Смешанные) 🟡

| Категория | Emoji | Описание |
|-----------|-------|----------|
| `exercise` | 🏃 | Спорт |
| `moderate_exercise` | 🚴 | Умеренный спорт |
| `social` | 👥 | Общение |
| `cooking` | 🍳 | Готовка |
| `projects` | 🎯 | Проекты |
| `shopping` | 🛒 | Покупки |
| `cleaning` | 🧽 | Наведение порядка |
| `planning` | 📅 | Планирование |
| `reading` | 📚 | Чтение |
| `volunteering` | 🤝 | Волонтерство |
| `spiritual` | 🕉️ | Духовные практики |

---

### 4. NEUTRAL (Нейтральные) ⚪

| Категория | Emoji | Длительность | Описание |
|-----------|-------|--------------|----------|
| `hygiene` | 🚿 | — | Гигиена |
| `waiting` | ⏳ | — | Ожидание |
| `testing` | 📊 | — | Тестирование |
| `practice` | 🧘 | — | Практика |
| `health` | 🩺 | — | Здоровье |
| `reflection` | 📝 | — | Рефлексия |
| `reflection_trackers_5min` | 📊 | 5 мин | Заполнить трекеры |
| `reflection_evening_10min` | 🌙 | 10 мин | Вечерняя рефлексия |
| `reflection_morning_10min` | 🌅 | 10 мин | Утренняя рефлексия |
| `other` | ⚡ | — | Другое |

---

## Временные слоты (Time Slots)

Активности могут быть привязаны к временным слотам дня:

```typescript
type TimeSlot = 'early_morning' | 'late_morning' | 'midday' | 
                'afternoon' | 'evening' | 'night' | 'anytime';
```

| Слот | Emoji | Часы | Описание |
|------|-------|------|----------|
| `early_morning` | 🌅 | 05:00 - 09:00 | Раннее утро |
| `late_morning` | ☕ | 09:00 - 12:00 | Позднее утро |
| `midday` | ☀️ | 12:00 - 15:00 | Середина дня |
| `afternoon` | 🌤️ | 15:00 - 18:00 | Послеобеденное время |
| `evening` | 🌆 | 18:00 - 22:00 | Вечер |
| `night` | 🌙 | 22:00 - 05:00 | Ночь |
| `anytime` | — | — | В любое время |

---

## Шаблоны активностей (Activity Templates)

### Структура в БД

```typescript
interface ActivityTemplate {
  id: string;                           // UUID
  name: string;                         // Название (legacy)
  name_en: string;                      // Английское название
  name_ru: string | null;               // Русское название
  name_fr: string;                      // Французское название
  description: string | null;           // Описание
  description_ru: string | null;        // Русское описание
  category: activity_category;          // Категория (enum)
  impact_type: activity_impact;         // Тип воздействия
  default_duration_minutes: number;     // Длительность по умолчанию
  emoji: string;                        // Эмодзи
  is_system: boolean;                   // Системный шаблон
  created_at: string;                   // Дата создания
}
```

### Особенности шаблонов

- **Системные шаблоны** (`is_system = true`) — создаются администратором
- **Локализация** — названия на EN/RU/FR
- **Связь с категорией** — каждый шаблон привязан к одной категории
- **Рекомендуемая длительность** — используется при создании активности

---

## Активности (Activities)

### Структура в БД

```typescript
interface Activity {
  id: string;                           // UUID
  user_id: string;                      // ID пользователя
  title: string;                        // Название
  description: string | null;           // Описание
  date: string;                         // Дата (YYYY-MM-DD)
  start_time: string | null;            // Время начала (HH:MM)
  end_time: string | null;              // Время окончания
  duration_minutes: number | null;      // Длительность (мин)
  category: activity_category;          // Категория
  impact_type: activity_impact;         // Тип воздействия
  status: activity_status;              // Статус
  priority: number;                     // Приоритет (1-5)
  emoji: string | null;                 // Эмодзи
  is_recurring: boolean;                // Повторяющаяся
  recurrence_pattern: Json | null;      // Паттерн повторения (legacy)
  repetition_config: Json | null;       // Конфигурация повторения
  reminder_enabled: boolean;            // Напоминание включено
  reminder_minutes_before: number;      // За сколько минут напоминать
  template_id: string | null;           // ID шаблона (если из шаблона)
  exercise_id: string | null;           // ID упражнения
  test_id: string | null;               // ID теста
  user_preset_id: string | null;        // ID пресета пользователя
  created_at: string;
  updated_at: string;
}
```

### Статусы активностей

```typescript
type activity_status = 'planned' | 'in_progress' | 'completed' | 'cancelled';
```

| Статус | Описание |
|--------|----------|
| `planned` | Запланирована |
| `in_progress` | Выполняется |
| `completed` | Выполнена |
| `cancelled` | Отменена |

---

## Пресеты (Presets)

### Системные пресеты

Определены в `src/config/activityPresets.ts`:

```typescript
interface ActivityPreset {
  id: string;
  name: { en: string; ru: string; fr: string };
  emoji: string;
  activities: PresetActivity[];
}
```

| ID | Название | Emoji | Описание |
|----|----------|-------|----------|
| `basic_needs` | Базовые потребности | 🔋 | Сон, питание, гидратация |
| `routines` | Рутины/привычки | 🔄 | Ежедневные привычки |
| `development` | Развитие | 📈 | Саморазвитие, обучение |
| `rest` | Отдых | 🌿 | Отдых и восстановление |
| `other` | Иное | ✨ | Прочее |

### Пользовательские пресеты

Хранятся в таблице `user_presets`:

```typescript
interface UserPreset {
  id: string;
  user_id: string;
  name: string;
  emoji: string;
  activities: PresetActivity[];
  tags: string[];
  is_active: boolean;
  is_archived: boolean;
  // Настройки повторения
  recurrence_type: string;
  recurrence_count: number;
  custom_interval: number;
  custom_unit: string;
  custom_end_type: string;
  custom_end_date: string;
  custom_end_count: number;
  // Даты активации
  activation_start_date: string | null;
  activation_end_date: string | null;
  last_activated_at: string | null;
}
```

### Теги пресетов

```typescript
const PRESET_TAGS = [
  'routine',      // 🔄 Рутина
  'care',         // 💝 Забота
  'health',       // 🏥 Здоровье
  'sport',        // 🏃 Спорт
  'habits',       // ✅ Привычки
  'tasks',        // 📋 Задачи
  'rest',         // 😴 Отдых
  'learning',     // 📚 Обучение
  'development',  // 🌱 Развитие
  'antistress',   // 🧘 Антистресс
  'basic_needs',  // 🏠 Базовые потребности
  'other'         // 📌 Другое
];
```

---

## Активность в пресете

```typescript
interface PresetActivity {
  template_id: string;      // ID шаблона
  category: string;         // Категория
  day_part: DayPart;        // Часть дня
  duration: number;         // Длительность (мин)
  repetitions: number;      // Количество повторений в день
}

type DayPart = 'early_morning' | 'late_morning' | 'midday' | 
               'afternoon' | 'evening' | 'night';
```

---

## Типы повторения

```typescript
type RecurrenceType = 'none' | 'daily' | 'weekly' | 'monthly' | 'custom';
```

### Конфигурация повторения

```typescript
interface RecurrenceConfig {
  type: RecurrenceType;
  count?: number;                  // Количество повторений
  customInterval?: number;         // Интервал (для custom)
  customUnit?: 'day' | 'week' | 'month' | 'year';
  customEndType?: 'never' | 'date' | 'count';
  customEndDate?: string;
  customEndCount?: number;
}
```

### Примеры повторения

| Тип | Настройка | Результат |
|-----|-----------|-----------|
| `daily` | count: 7 | 7 дней подряд |
| `weekly` | count: 4 | Каждую неделю 4 раза |
| `monthly` | count: 6 | Каждый месяц 6 раз |
| `custom` | interval: 2, unit: day | Каждые 2 дня |
| `custom` | interval: 1, unit: week, endType: date | Еженедельно до даты |

---

## Утилиты

### getCategoriesByType

```typescript
getCategoriesByType(type: ImpactType): CategoryConfig[]
```

Возвращает все категории указанного типа воздействия.

### getCategoryConfig

```typescript
getCategoryConfig(categoryKey: string): CategoryConfig | undefined
```

Возвращает конфигурацию категории по ключу.

### generateRecurrenceDates

```typescript
generateRecurrenceDates(startDate: Date, settings: RecurrenceSettings): string[]
```

Генерирует массив дат на основе настроек повторения.

### getDefaultTimeForSlot

```typescript
getDefaultTimeForSlot(slot: TimeSlot): string | null
```

Возвращает время начала для временного слота (например, `"09:00"` для `late_morning`).

---

## Связи между сущностями

```
┌─────────────────────┐
│  activity_templates │ ─── Системные шаблоны (БД)
└──────────┬──────────┘
           │ template_id
           ▼
┌─────────────────────┐
│     activities      │ ─── Конкретные активности пользователя
└──────────┬──────────┘
           │ user_preset_id
           ▼
┌─────────────────────┐
│    user_presets     │ ─── Пресеты пользователя
└─────────────────────┘
           │
           ▼
┌─────────────────────┐
│   categoryConfig    │ ─── Конфигурация категорий (фронтенд)
└─────────────────────┘
```

---

## Диаграмма типов

```
                    ┌──────────────┐
                    │ ImpactType   │
                    └──────┬───────┘
                           │
        ┌──────────────────┼──────────────────┐
        ▼                  ▼                  ▼
   ┌─────────┐       ┌──────────┐       ┌─────────┐
   │restoring│       │ depleting│       │  mixed  │
   └────┬────┘       └────┬─────┘       └────┬────┘
        │                 │                  │
   ┌────┴────┐       ┌────┴────┐        ┌────┴────┐
   │ Groups  │       │  No     │        │  No     │
   └─────────┘       │ groups  │        │ groups  │
   - sleep           └─────────┘        └─────────┘
   - nutrition
   - hydration
   - rest_practices
   - social_hobbies
```
