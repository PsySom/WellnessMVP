# Сценарии пользовательского опыта активностей

## Общее описание

Документ описывает все сценарии взаимодействия пользователя с системой активностей, шаблонов и их связи с трекерами и упражнениями.

---

## Точки входа

```
┌─────────────────────────────────────────────────────────────────┐
│                        Главное меню                              │
├─────────────────────────────────────────────────────────────────┤
│  📅 Calendar       │  📋 Activity Templates  │  🏠 Dashboard     │
│  (Календарь)       │  (Шаблоны активностей)  │  (Главная)        │
└─────────────────────────────────────────────────────────────────┘
         │                      │                      │
         ▼                      ▼                      ▼
   Управление           Создание/редактир.      Быстрый обзор
   активностями         пресетов               сегодняшних активностей
```

---

## Сценарий 1: Создание активности вручную

### Путь пользователя

```
Calendar → [+] Кнопка → ActivityFormModal → Заполнение → Сохранение
```

### Детальный поток

```
┌─────────────────────────────────────────────────────────────────┐
│ 1. Пользователь открывает Calendar                               │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│ 2. Нажимает [+] на выбранную дату                               │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│ 3. Открывается ActivityFormModal                                 │
│    ┌─────────────────────────────────────────────────────────┐  │
│    │ • Выбор типа воздействия (restoring/depleting/mixed)    │  │
│    │ • Выбор категории (фильтруется по типу)                 │  │
│    │ • Название (опционально)                                │  │
│    │ • Дата и временной слот                                 │  │
│    │ • Длительность (подставляется из категории)             │  │
│    │ • Приоритет (1-5)                                       │  │
│    │ • Настройки повторения                                  │  │
│    │ • Напоминание                                           │  │
│    └─────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│ 4. При выборе повторения:                                        │
│    • none - одна активность                                      │
│    • daily - N дней подряд                                       │
│    • weekly - N недель                                           │
│    • monthly - N месяцев                                         │
│    • custom - пользовательские настройки                         │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│ 5. Генерация активностей:                                        │
│    generateRecurrenceDates(startDate, settings) → dates[]        │
│    → Создание activities для каждой даты                         │
│    → Связывание через recurrence_group_id                        │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│ 6. Сохранение в БД:                                              │
│    INSERT INTO activities (...) VALUES (...)                     │
│    → triggerActivityUpdate() для обновления UI                   │
└─────────────────────────────────────────────────────────────────┘
```

---

## Сценарий 2: Создание активности из шаблона

### Путь пользователя

```
Calendar → TemplatesSidebar → Клик на шаблон → ActivityFormModal (предзаполнен)
```

### Детальный поток

```
┌─────────────────────────────────────────────────────────────────┐
│ 1. Пользователь в Calendar, видит боковую панель шаблонов        │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│ 2. Фильтрация шаблонов:                                          │
│    • По типу воздействия (all/restoring/depleting/mixed/neutral) │
│    • По системным пресетам (basic_needs, routines, etc.)         │
│    • По типу активности (core/additional)                        │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│ 3. Клик на шаблон:                                               │
│    onOpenActivityModal(template) →                               │
│    ActivityFormModal открывается с предзаполненными данными:     │
│    • title = template.name_[locale]                              │
│    • category = template.category                                │
│    • impact_type = template.impact_type                          │
│    • duration = template.default_duration_minutes                │
│    • emoji = template.emoji                                      │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│ 4. Быстрое добавление (без модала):                              │
│    Клик на [+] иконку шаблона →                                  │
│    Проверка на дубликаты →                                       │
│    Если дубликат: показ диалога подтверждения                    │
│    Если нет: мгновенное добавление активности                    │
└─────────────────────────────────────────────────────────────────┘
```

---

## Сценарий 3: Работа с пресетами

### 3.1 Создание пользовательского пресета

```
Activity Templates → [+] Новый пресет → Drag & Drop шаблонов → Сохранение
```

```
┌─────────────────────────────────────────────────────────────────┐
│ Страница Activity Templates (/activity-templates)               │
├────────────────────────┬────────────────────────────────────────┤
│  Левая панель          │  Правая панель                         │
│  (Шаблоны)             │  (Редактор пресета)                    │
├────────────────────────┼────────────────────────────────────────┤
│  • Поиск               │  • Название пресета                    │
│  • Фильтр по типу      │  • Теги                                │
│  • Список шаблонов     │  • Настройки повторения                │
│    (drag source)       │  • Зоны времени дня:                   │
│                        │    🌅 Раннее утро                      │
│                        │    ☕ Позднее утро                     │
│                        │    ☀️ Середина дня                     │
│                        │    🌤️ Послеобеденное                   │
│                        │    🌆 Вечер                            │
│                        │    🌙 Ночь                             │
│                        │  (drop zones)                          │
└────────────────────────┴────────────────────────────────────────┘
```

### Drag & Drop логика

```typescript
// Перетаскивание шаблона в зону времени
handleTemplateDragStart(e, template) → setDraggedTemplate(template)
handleDayPartDrop(e, dayPart) → {
  newActivity = {
    template_id: draggedTemplate.id,
    category: draggedTemplate.category,
    day_part: dayPart,
    duration: draggedTemplate.default_duration_minutes,
    repetitions: 1
  }
  setActivities([...activities, newActivity])
}

// Перемещение активности между зонами
handleActivityDragStart(e, index) → setDraggedActivityIndex(index)
handleDayPartDrop(e, dayPart) → {
  updatedActivities[index].day_part = dayPart
}

// Удаление активности
handleRemoveZoneDrop(e) → removeActivity(draggedActivityIndex)
```

### 3.2 Активация пресета

```
Activity Templates / Calendar Sidebar → Play ▶ → Подтверждение → Генерация активностей
```

```
┌─────────────────────────────────────────────────────────────────┐
│ activateMutation:                                                │
│                                                                  │
│ 1. Получить настройки повторения из пресета:                     │
│    recurrence_type, recurrence_count, custom_*                   │
│                                                                  │
│ 2. Сгенерировать даты:                                           │
│    generateRecurrenceDates(startDate, settings) → dates[]        │
│                                                                  │
│ 3. Для каждой даты и каждой активности в пресете:                │
│    - Найти шаблон по template_id                                 │
│    - Создать активность с:                                       │
│      • user_preset_id = preset.id (связь с пресетом)             │
│      • date = текущая дата из массива                            │
│      • start_time = getDefaultTimeForSlot(activity.day_part)     │
│                                                                  │
│ 4. Batch insert в activities (по 100 записей)                    │
│                                                                  │
│ 5. Обновить пресет:                                              │
│    is_active = true                                              │
│    activation_start_date = startDate                             │
│    activation_end_date = lastDate                                │
│    last_activated_at = now()                                     │
└─────────────────────────────────────────────────────────────────┘
```

### 3.3 Деактивация пресета

```
┌─────────────────────────────────────────────────────────────────┐
│ deactivateMutation:                                              │
│                                                                  │
│ 1. DELETE FROM activities WHERE user_preset_id = preset.id       │
│    (Удаляются ВСЕ активности, созданные этим пресетом)           │
│                                                                  │
│ 2. UPDATE user_presets SET                                       │
│    is_active = false,                                            │
│    activation_start_date = null,                                 │
│    activation_end_date = null                                    │
│    WHERE id = preset.id                                          │
└─────────────────────────────────────────────────────────────────┘
```

### 3.4 Архивация пресета

```
┌─────────────────────────────────────────────────────────────────┐
│ archiveMutation:                                                 │
│                                                                  │
│ 1. DELETE FROM activities WHERE user_preset_id = preset.id       │
│                                                                  │
│ 2. UPDATE user_presets SET                                       │
│    is_archived = true,                                           │
│    is_active = false                                             │
│    WHERE id = preset.id                                          │
│                                                                  │
│ → Пресет перемещается во вкладку "Архив"                         │
│ → Можно восстановить (restoreMutation)                           │
└─────────────────────────────────────────────────────────────────┘
```

---

## Сценарий 4: Редактирование активности

### 4.1 Редактирование одиночной активности

```
Calendar → Клик на активность → ActivityDetailModal → [Edit] → ActivityFormModal
```

```
┌─────────────────────────────────────────────────────────────────┐
│ 1. Клик на активность в календаре                               │
│    → Открывается ActivityDetailModal                            │
│    → Показывает: название, время, категорию, статус             │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│ 2. Кнопка [Edit]                                                 │
│    → Открывается ActivityFormModal с заполненными данными        │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│ 3. Редактирование и сохранение                                   │
│    → UPDATE activities SET ... WHERE id = activity.id            │
└─────────────────────────────────────────────────────────────────┘
```

### 4.2 Редактирование повторяющейся активности

```
┌─────────────────────────────────────────────────────────────────┐
│ При сохранении проверяется:                                      │
│ • hasGroup = isRecurring || isFromPreset                         │
│                                                                  │
│ Если hasGroup = true:                                            │
│ → Показывается диалог выбора:                                    │
│   ┌─────────────────────────────────────────────────────────┐   │
│   │ Редактировать:                                          │   │
│   │ ○ Только эту активность                                 │   │
│   │ ○ Все активности в группе                               │   │
│   └─────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
                              │
           ┌──────────────────┴──────────────────┐
           ▼                                     ▼
┌─────────────────────┐             ┌─────────────────────┐
│ "Только эту"        │             │ "Все в группе"      │
│                     │             │                     │
│ UPDATE activities   │             │ 1. SELECT * WHERE   │
│ WHERE id = this.id  │             │    recurrence_group │
│                     │             │    OR user_preset_id│
│                     │             │                     │
│                     │             │ 2. UPDATE activities│
│                     │             │    WHERE id IN (...)|
└─────────────────────┘             └─────────────────────┘
```

---

## Сценарий 5: Изменение статуса активности

### Quick Actions

```
┌─────────────────────────────────────────────────────────────────┐
│ В ActivityDetailModal или ActivityItem:                          │
│                                                                  │
│ [▶ Start]     → status = 'in_progress'                           │
│ [✓ Complete]  → status = 'completed'                             │
│ [✗ Cancel]    → status = 'cancelled'                             │
│ [↺ Reset]     → status = 'planned'                               │
└─────────────────────────────────────────────────────────────────┘
```

### Визуальная индикация статусов

```
┌─────────────┬────────────────────────────────────────────────────┐
│ planned     │ Обычный вид, полная непрозрачность                 │
├─────────────┼────────────────────────────────────────────────────┤
│ in_progress │ Анимированная граница, индикатор прогресса         │
├─────────────┼────────────────────────────────────────────────────┤
│ completed   │ Зачеркнутый текст, галочка, зеленый акцент         │
├─────────────┼────────────────────────────────────────────────────┤
│ cancelled   │ Полупрозрачный, перечеркнутый                      │
└─────────────┴────────────────────────────────────────────────────┘
```

---

## Сценарий 6: Удаление активности

```
┌─────────────────────────────────────────────────────────────────┐
│ ActivityDetailModal → [Delete]                                   │
│                                                                  │
│ Если активность в группе (recurring/preset):                     │
│ → Показывается диалог:                                           │
│   ┌─────────────────────────────────────────────────────────┐   │
│   │ Удалить:                                                │   │
│   │ ○ Только эту активность                                 │   │
│   │ ○ Все активности в группе                               │   │
│   └─────────────────────────────────────────────────────────┘   │
│                                                                  │
│ DELETE FROM activities WHERE ...                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## Сценарий 7: Связь с упражнениями

### Добавление упражнения в расписание

```
Exercises → ExerciseDetail → [Add to Schedule] → ActivityFormModal
```

```
┌─────────────────────────────────────────────────────────────────┐
│ ExerciseDetail:                                                  │
│                                                                  │
│ const handleAddToSchedule = () => {                              │
│   navigate('/calendar', {                                        │
│     state: {                                                     │
│       openActivityModal: true,                                   │
│       exerciseId: exercise.id,                                   │
│       initialValues: {                                           │
│         title: getLocalizedName(exercise),                       │
│         duration_minutes: exercise.duration_minutes,             │
│         category: 'rest_psychological_exercises',                │
│         impact_type: 'restoring',                                │
│         emoji: exercise.emoji                                    │
│       }                                                          │
│     }                                                            │
│   });                                                            │
│ };                                                               │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│ Calendar получает state и:                                       │
│ 1. Открывает ActivityFormModal                                   │
│ 2. Передает exerciseId и initialValues                           │
│ 3. При сохранении activity.exercise_id = exerciseId              │
└─────────────────────────────────────────────────────────────────┘
```

### Выполнение упражнения из активности

```
┌─────────────────────────────────────────────────────────────────┐
│ ActivityDetailModal (если activity.exercise_id):                 │
│                                                                  │
│ [Start Exercise] → navigate(`/exercises/${exerciseSlug}/session`)│
│                                                                  │
│ После завершения сессии:                                         │
│ → INSERT INTO exercise_sessions (...)                            │
│ → UPDATE activities SET status = 'completed'                     │
└─────────────────────────────────────────────────────────────────┘
```

---

## Сценарий 8: Связь с тестами

### Добавление теста в расписание

```
Tests → TestDetail → [Add to Schedule] → ActivityFormModal
```

```
┌─────────────────────────────────────────────────────────────────┐
│ TestDetail:                                                      │
│                                                                  │
│ const handleAddToSchedule = () => {                              │
│   navigate('/calendar', {                                        │
│     state: {                                                     │
│       openActivityModal: true,                                   │
│       testId: test.id,                                           │
│       initialValues: {                                           │
│         title: getLocalizedName(test),                           │
│         duration_minutes: test.duration_minutes,                 │
│         category: 'testing',                                     │
│         impact_type: 'neutral',                                  │
│         emoji: '📊'                                              │
│       }                                                          │
│     }                                                            │
│   });                                                            │
│ };                                                               │
└─────────────────────────────────────────────────────────────────┘
```

### Прохождение теста из активности

```
┌─────────────────────────────────────────────────────────────────┐
│ ActivityDetailModal (если activity.test_id):                     │
│                                                                  │
│ [Take Test] → navigate(`/tests/${testSlug}/take`)                │
│                                                                  │
│ После завершения:                                                │
│ → INSERT INTO test_results (...)                                 │
│ → UPDATE activities SET status = 'completed'                     │
└─────────────────────────────────────────────────────────────────┘
```

---

## Сценарий 9: Связь с трекерами

### Автоматическое создание активности для трекера

```
┌─────────────────────────────────────────────────────────────────┐
│ Настройки профиля (Profile → Tracker Settings):                  │
│                                                                  │
│ tracker_frequency: 2                                             │
│ tracker_times: ["09:00", "21:00"]                                │
│                                                                  │
│ При активации настроек:                                          │
│ → Создаются активности с category = 'reflection_trackers_5min'   │
│ → Устанавливаются напоминания                                    │
└─────────────────────────────────────────────────────────────────┘
```

### Категории трекинга в активностях

| Категория | Описание | Длительность |
|-----------|----------|--------------|
| `reflection_trackers_5min` | Заполнить трекеры | 5 мин |
| `reflection_morning_10min` | Утренняя рефлексия | 10 мин |
| `reflection_evening_10min` | Вечерняя рефлексия | 10 мин |

### Выполнение трекера из активности

```
┌─────────────────────────────────────────────────────────────────┐
│ ActivityDetailModal (если category = 'reflection_*'):            │
│                                                                  │
│ [Fill Tracker] → navigate('/tracker-history')                    │
│                  или открытие QuickTrackerCard на Dashboard      │
│                                                                  │
│ После заполнения:                                                │
│ → INSERT INTO tracker_entries (...)                              │
│ → INSERT INTO tracker_emotions (...)                             │
│ → UPDATE activities SET status = 'completed'                     │
└─────────────────────────────────────────────────────────────────┘
```

---

## Сценарий 10: Dashboard - Быстрый обзор

### Карточка "Сегодняшние активности"

```
┌─────────────────────────────────────────────────────────────────┐
│ TodayActivitiesCard:                                             │
│                                                                  │
│ Query: activities WHERE date = today AND user_id = current_user  │
│                                                                  │
│ Отображение:                                                     │
│ ┌─────────────────────────────────────────────────────────────┐ │
│ │ 🌅 08:00 - Утренняя зарядка          [✓]                    │ │
│ │ 🍳 09:00 - Завтрак                   [✓]                    │ │
│ │ 💼 10:00 - Работа                    [▶]                    │ │
│ │ 🍽️ 13:00 - Обед                      [ ]                    │ │
│ │ 📊 18:00 - Заполнить трекеры         [ ]                    │ │
│ │ 🌙 22:00 - Вечерняя рефлексия        [ ]                    │ │
│ └─────────────────────────────────────────────────────────────┘ │
│                                                                  │
│ [View Calendar] → navigate('/calendar')                          │
└─────────────────────────────────────────────────────────────────┘
```

### Activity Impact Cards

```
┌─────────────────────────────────────────────────────────────────┐
│ ActivityImpactCards:                                             │
│                                                                  │
│ Статистика за сегодня/неделю:                                    │
│                                                                  │
│ ┌────────────┐ ┌────────────┐ ┌────────────┐ ┌────────────┐     │
│ │ 🟢 Restore │ │ 🔴 Deplete │ │ 🟡 Mixed   │ │ ⚪ Neutral │     │
│ │    4h      │ │    6h      │ │    2h      │ │    1h      │     │
│ │   32%      │ │   46%      │ │   15%      │ │    7%      │     │
│ └────────────┘ └────────────┘ └────────────┘ └────────────┘     │
│                                                                  │
│ Баланс: энергии расходуется больше чем восстанавливается        │
│ Рекомендация: добавить восстанавливающие активности             │
└─────────────────────────────────────────────────────────────────┘
```

---

## Сценарий 11: Рекомендации активностей

### Генерация рекомендаций

```
┌─────────────────────────────────────────────────────────────────┐
│ Edge Function: generate-recommendations                          │
│                                                                  │
│ 1. Анализ последних tracker_entries:                             │
│    - mood_score, stress_level, anxiety_level, energy_level       │
│                                                                  │
│ 2. Проверка recommendation_rules:                                │
│    SELECT * FROM recommendation_rules WHERE enabled = true       │
│                                                                  │
│ 3. Для каждого правила проверить trigger_condition:              │
│    {                                                             │
│      "metric": "stress_level",                                   │
│      "operator": ">=",                                           │
│      "value": 7                                                  │
│    }                                                             │
│                                                                  │
│ 4. Если условие выполнено:                                       │
│    INSERT INTO user_recommendations (                            │
│      user_id,                                                    │
│      activity_template_id,                                       │
│      reason,                                                     │
│      priority                                                    │
│    )                                                             │
└─────────────────────────────────────────────────────────────────┘
```

### Отображение рекомендаций

```
┌─────────────────────────────────────────────────────────────────┐
│ Recommendations Page (/recommendations):                         │
│                                                                  │
│ ┌─────────────────────────────────────────────────────────────┐ │
│ │ 🧘 Рекомендуем: Медитация                                   │ │
│ │ Причина: Высокий уровень стресса                            │ │
│ │ [Добавить в расписание] [Отклонить]                         │ │
│ └─────────────────────────────────────────────────────────────┘ │
│ ┌─────────────────────────────────────────────────────────────┐ │
│ │ 🚶 Рекомендуем: Прогулка                                    │ │
│ │ Причина: Низкий уровень энергии                             │ │
│ │ [Добавить в расписание] [Отклонить]                         │ │
│ └─────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
```

---

## Сценарий 12: Напоминания

### Настройка напоминаний

```
┌─────────────────────────────────────────────────────────────────┐
│ ActivityFormModal → Reminder Settings:                           │
│                                                                  │
│ [✓] Включить напоминание                                         │
│ За: [15] минут до начала                                         │
│                                                                  │
│ Сохраняется:                                                     │
│ reminder_enabled = true                                          │
│ reminder_minutes_before = 15                                     │
└─────────────────────────────────────────────────────────────────┘
```

### Глобальные настройки напоминаний

```
┌─────────────────────────────────────────────────────────────────┐
│ Profile → Notification Settings:                                 │
│                                                                  │
│ activity_reminders_enabled = true                                │
│ activity_reminder_minutes = 15                                   │
│                                                                  │
│ Эти настройки применяются как default для новых активностей      │
└─────────────────────────────────────────────────────────────────┘
```

---

## Диаграмма потоков данных

```
┌──────────────────────────────────────────────────────────────────────────┐
│                              ПОЛЬЗОВАТЕЛЬ                                │
└───────────────────────────────────┬──────────────────────────────────────┘
                                    │
        ┌───────────────────────────┼───────────────────────────┐
        ▼                           ▼                           ▼
┌───────────────┐          ┌───────────────┐          ┌───────────────┐
│   Dashboard   │          │   Calendar    │          │   Templates   │
└───────┬───────┘          └───────┬───────┘          └───────┬───────┘
        │                          │                          │
        │ view                     │ CRUD                     │ manage
        │                          │                          │
        ▼                          ▼                          ▼
┌──────────────────────────────────────────────────────────────────────────┐
│                              activities                                   │
│  ┌─────────────────────────────────────────────────────────────────────┐ │
│  │ id, user_id, title, date, start_time, category, impact_type,        │ │
│  │ status, exercise_id, test_id, user_preset_id, repetition_config     │ │
│  └─────────────────────────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────────────────────────┘
        │                          │                          │
        │ template_id              │ exercise_id              │ test_id
        ▼                          ▼                          ▼
┌───────────────┐          ┌───────────────┐          ┌───────────────┐
│   activity    │          │   exercises   │          │     tests     │
│   templates   │          │               │          │               │
└───────────────┘          └───────┬───────┘          └───────┬───────┘
                                   │                          │
                                   │ sessions                 │ results
                                   ▼                          ▼
                           ┌───────────────┐          ┌───────────────┐
                           │   exercise    │          │  test_results │
                           │   sessions    │          │               │
                           └───────────────┘          └───────────────┘
                                   │
                                   │ mood tracking
                                   ▼
                           ┌───────────────────────────────────────────┐
                           │              tracker_entries              │
                           │  + tracker_emotions                       │
                           └───────────────────────────────────────────┘
                                   │
                                   │ triggers recommendations
                                   ▼
                           ┌───────────────────────────────────────────┐
                           │           user_recommendations            │
                           │  → activity_templates                     │
                           └───────────────────────────────────────────┘
```

---

## Состояния UI компонентов

### CalendarView

```
┌─────────────────────────────────────────────────────────────────┐
│ States:                                                          │
│                                                                  │
│ isLoading     → Показывает skeleton/spinner                      │
│ error         → Показывает сообщение об ошибке                   │
│ activities[]  → Группируются по date и timeSlot                  │
│ selectedDate  → Дата для добавления новых активностей            │
│ viewMode      → 'calendar' | 'list'                              │
└─────────────────────────────────────────────────────────────────┘
```

### ActivityFormModal

```
┌─────────────────────────────────────────────────────────────────┐
│ States:                                                          │
│                                                                  │
│ formData      → Все поля формы                                   │
│ loading       → Блокирует кнопку сохранения                      │
│ isEditing     → Режим редактирования                             │
│ hasGroup      → Показывает диалог выбора при сохранении          │
│ editMode      → 'single' | 'all'                                 │
│ duplicateDialog → Подтверждение добавления дубликата             │
└─────────────────────────────────────────────────────────────────┘
```

### UserPreset

```
┌─────────────────────────────────────────────────────────────────┐
│ States:                                                          │
│                                                                  │
│ is_active     → Показывает Play/Stop кнопку                      │
│ is_archived   → Перемещает во вкладку "Архив"                    │
│ editingPreset → Загружает данные в редактор                      │
│                                                                  │
│ Визуальная индикация:                                            │
│ • Активный: зеленая граница, кнопка Stop                         │
│ • Неактивный: обычная граница, кнопка Play                       │
│ • Архивный: серый, кнопки Restore/Delete                         │
└─────────────────────────────────────────────────────────────────┘
```

---

## Реактивность и синхронизация

### triggerActivityUpdate

```typescript
// utils/activitySync.ts
export const triggerActivityUpdate = () => {
  window.dispatchEvent(new CustomEvent('activity-update'));
};

// Используется после:
// - Создания активности
// - Обновления активности
// - Удаления активности
// - Активации/деактивации пресета

// Подписка в компонентах:
useEffect(() => {
  const handler = () => queryClient.invalidateQueries(['activities']);
  window.addEventListener('activity-update', handler);
  return () => window.removeEventListener('activity-update', handler);
}, []);
```

### Query Invalidation

```typescript
// После мутаций автоматически инвалидируются:
queryClient.invalidateQueries({ queryKey: ['activities'] });
queryClient.invalidateQueries({ queryKey: ['user-presets'] });
```
